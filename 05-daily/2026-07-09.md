# 2026-07-09

## Session — v3.1 promote/release cx K-BB% shipped (Opus 4.8)
- Shipped **v3.1**: cx (DSL/FCL) pitcher quality **K% → K-BB%** in both models (release_v3/promote_v3), flips feature + label. Reverses the "walks are zero signal at complex" finding — it was a **circular-label artifact** (the label was built from the walk-blind metric). Validated on the FULL production equations (release +0.029/+0.035, promote +0.039, bootstrap CIs exclude 0); weights re-optimized (frozen 0.607/N0=50 stay); feature-importance measured (quality more important; whiff%/stuff+ lose share; promote drops the level term; projection unchanged).
- **KEY architecture lesson:** the promo-engine BOARD = **v3 scorers** (`score_active_roster_v3.py` + `_release_v3.py`, fit-at-score-time), NOT `train_winners` (a separate v1/winners system writing a different pin). I gave the wrong deploy runbook first (train_winners/DEPLOY.md) and corrected it to the 2 v3 scorers; added a DEPLOY.md scope banner.
- Deploy v3.1 = `git pull` → `score_active_roster_v3.py` → `score_active_roster_release_v3.py`.
- Also: spring-2026 release backtest (model saw 20/22 cuts) + "kept but flagged" board delivered; shareable Word doc with a 7-stage model-update process; fixed the app driver table (K%→K-BB%).
- Commits `2f23f289`/`8d30aafc`/DEPLOY-banner/app-fix/spec on `feature/promotion-models`.
- **Remaining (next session, spec'd):** deploy-verify, PA/IP (Lvl) null fix (dump null players on work laptop first), rebuild the end-user PDF (rerun complex SHAP + ROC/AUC curves, re-describe K-BB%, simplify), full `/code-review ultra`.
- **Zac wants remembered:** document everything (kept asking for /document — now have a decision log + next-session spec + Word doc); recreate the end-user PDF simple + rerun SHAP/ROC for complex.

## Session — context-bloat diagnosis (Opus 4.8)
- Diagnosed WHY fresh Claude Code sessions burn ~50% context before real work: `.claude/rules` auto-loader (1.1MB/84 files) dumps ~42 rule files (~140K tok) on a couple `pd-goals` file reads + `settings.local.json` is 27K tok of individual permission entries. The knowledge-preservation system became the #1 context hog (eager injection vs retrieval).
- 4-lever fix plan captured in `meta/context-bloat-fix-plan.md`. Highest leverage = tighten rule auto-load scope (~5 rules not 42) + collapse settings.local.json to wildcards.
- Decision: `/wrap` + `/clear`, then `/spec` the fix fresh next session.
- Earlier same session: shipped zero-count NaN fix, org-kpi failure-surfacing, run_monday advance-first reorder, Brandon Nigh zzz_ channel, weekly-hitter L2W skip + new `weekly-report-no-activity-gate.md` rule, skills cheat-sheet (`meta/skills-cheatsheet.md`) + memory pointer.

## Session — slack z-channels + FF/FT 93.5 velo queries
- Added athlete z-channels: Ryan Weiss 80640 (C0A3LQ6U8Q5), Julio Rodriguez 82042 (C085WKHS0D9) into their zzz_ rows, all 5 CSVs, pushed 4 worktrees. z-total now 243.
- DNS false alarm: "did you delete all the z-channels?!" — NO. Work-laptop postgame run had 66 delivery FAILs, all `getaddrinfo failed` = VPN/DNS dropped mid-run. "Loaded 243 channel mappings" proves channels intact. Re-run when network back.
- Built 2 SQL deliverables (sql-queries/, uncommitted): HOU MiLB (AAA/AA/A+/A/FCL) pitchers avg >=93.5 on FF or FT — player/#ofP/FF Velo/FT Velo, top row = qual/totalArms; roster gate drops MLB rehabbers (LEVELOFPLAY<>'ml') + released (status/ORG_LK). Plus a 30-org leaderboard version (org-code CASE remap so all 30 show).
- **Remember:** for cross-org "arms" leaderboards, org attribution = fielding-team org via mlbam.teams + BLOCKING org-code canon (OAK->ATH, CHI/LA/NY->CHC/LAD/NYM on both sides). Roster gate to exclude rehabbers = LEVELOFPLAY_LK<>'ml'; released = ORG_LK/status filter.
- User ended with "now i have some fun stuff for you" → new task incoming.


## Ryan Isler — Command Training app live on Posit (2026-07-08)
Hosted Ryan's updated R/Shiny Command Training app to connect2 under Zac's account
(Ryan lost publishing rights → he pushes GitHub, Zac publishes, Ryan = viewer).
Repo `zbridger_astros/command-training-isler` (fresh, no rulesets), r-resources kit
pushed alongside. Full reusable playbook → [[r-posit-publishing]]. Follow-up:
bundled-SQLite writes don't persist on Connect (durable store = next iteration).

## Session: Savant fielding visuals (Mazzo) — full GSD build + review loop
- Built OF+IF Range & Difficulty (catch-difficulty stars + reach-ring bullseye) end-to-end: /kickoff → 6 phases → /reviewloop. ~30 commits pushed on feature/astros-intangibles; app cards + weekly-PDF pages; all render-and-look gated.
- 19-agent review: 10 confirmed findings, 8 fixed (incl. FEATURE-KILLER: IF views used the OF-only game resolver — every pure infielder rendered empty), 2 deferred (query-template hoist, video-fallback dedup).
- DECISIONS: star scale = existing EO buckets (Elite 5★…Routine 1★); Wall=wall_ball; Back from direction_txt; top_speed is ft/s (NEVER convert); theta 0=RF/90=LF (old docstring wrong); IF time axis = xtime_to_fielder w/ reaction fallback.
- **Zac: weekly-PDF page GATED on Mazzo approval** — no delivered weekly includes it until he signs off. Also pending: Mazzo reads on OF ring geometry + the novel IF ball-time rings.
- Resume: work-laptop redeploy (GUID 295a5205) + UAT sweep (~23 items) + backlog rows 14-16 + Sullivan (218708) first PDF test.

## Command CV — video → pitcher command (afternoon session)
Took Command CV from 49 hand-labels to a fully automatic, validated, season-scale metric.
- Trained YOLO glove detector (mAP50 .85); auto setup-detect = 2.3px, catch via glove-motion + RANSAC salvage.
- ① TIGHTEN: hands-off calibration LOO **3.9in** (beats 4.2in hand baseline). ② SCALE: Ogando full season → **110-pitch command board, 14.2in median** (FB tighter than breaking balls).
- **plate_x sign LOCKED** via si_12 HBP-on-RHB → neg plate_x = 3B = RHP arm-side.
- Delivered: 15 annotated command videos, command grid, sign-check grid, published Astros dossier.
- Documented two-model / two-plane framework: Model A (Trackman-backed, ship first) vs Model B (standalone CV, the goal) + future A↔B discrepancy learner.
- Decisions: BIP/dirt/foul all get command miss (outcome-independent; actual = Trackman data, never a video mark). Glove model trained on ONE catcher pool → needs multi-catcher annotation to scale. Arm Farm integration = separate /spec + /plan LATER (parked).
- NEXT: Zac annotates 69 hard-case clips (retrain_hard_cases.txt) → retrain.

## Session — PD-Goals un-wired metrics wired + goal-parser audit (Opus 4.8)
- Wired 4 backlog-#20 metrics end-to-end (value/parser/pool/rolling/chart, the 5-place pattern): **SB% success rate** (`4cb7c0e9`), **hitter GB%** (`4cb7c0e9`), **FT/CU per-PT velo** + **CH-usage-overall** (`6b0a9648`). Every parser change golden-diffed over all 272 goals = 0 changed parses.
- Shipped an **`avg velo` word-order normalizer** ("FF avg velo"/"avg FT velo"/"avg velo on the SL" → `<PT> Velo`) — fixes the detect_goal_type trap where FT/CU velo fell to HITTING because `pitching_indicators` lacked `ft vel`/`cu vel`.
- **Parenthetical fix** (`311ba1d8`): `(currently 32.6)` now stripped at DISPLAY too via shared `strip_current_value_paren` (report.py + 1_PD_Goals.py), narrowed to curr+digit so `(maintain current weight)` is preserved. Value was already correct (target 40, not 32.6).
- Goal-parser AUDIT of Zac's flagged list: **handedness (LHH/RHH/LHP/RHP) works** (extract_platoon_from_text); **OF/IF works** (position_override, `ALL` default). Confirmed BROKEN (raw phrase → N/A) = the un-wired set: breaking-ball InZ (dangerously parses to generic InZ%), arm-angle+range, PL1B, shape "8,8", RelX, Attack Angle, `CH Usg`/`FC v RHH`/`Pre2K IZ` aliases.
- Read-only verifier `pd-goals/scripts/verify_new_metrics.py` (`7f6ef1d6`). Resume plan `docs/plans/2026-07-09-remaining-goal-metrics-implementation-plan.md` (`f0c74dab`) — ordered, exact touchpoints, for /brief.
- **Zac decisions:** SB% pool gate = 10 attempts. Batch goals audit (many gcids, one run, dump metric/value/pctile/met/NA) = the DB-verify vehicle, parked to /spec later; verify_new_metrics.py is its seed.
- NEXT: work-laptop DB-verify the 4 shipped metrics, then execute the remaining-metrics plan starting with breaking-ball InZ.

## Session — promo-engine board redesign + model studies (wrap 16:57)
- **Board redesign SHIPPED** (feature/promotion-models `a032126c`→`102c9be4`): killed the Grade-basis toggle → 4 labeled columns **Promote | Org %ile (Pro) | Release | Org %ile (Rel)**. Org %ile = all-org percentile (in-app, no re-pin) fixes the within-side leapfrog (low-odds pitcher was outranking high-odds hitter). Fixed a regression: Promote must use GRADE_ABS (masks thin) not raw odds_pct. Removed Sort-by/Order controls. NEXT = Zac redeploy + eyeball.
- **Council studies** (discovery/, uncommitted): sample size does NOT predict promotion success (floor = reliability only); release model has real signal (mistake-releases Stubbs/Perez/Kato/Stevens/Santana hit at AA/AAA elsewhere on middling risk); promote HONEST but blind at AAA→MLB (Blanco/Arrighetti/Bielak reached MLB from bottom-half grades). Move-stats table = at-time-of-transaction, HOU-to-HOU promotes, (year) on names, real 2026 names (Winkler/Willingham/Oakes/Brutcher/Schiavone/Ferreras).
- **Big finding + next-session workstream:** the COMPLEX (DSL/FCL) pitcher model uses `k_pct` not `k_bb_pct`/`bb_pct` = blind to walks → missed walk-driven releases (Maican, De La Cruz DSL K-BB -2.2 graded 52, E.Gonzalez). Willingham (AAA, K-BB -3.4) WAS flagged (93rd) because the milb model uses K-BB%. Fix = add command to cx model + validate. Spec = `modeling/docs/plans/2026-07-09-NEXT-SESSION-spec.md`.
- **Remember (Zac):** 2026 data comes from Barrelsville+Arm Farm tracker pins (`run_feature_etl --years ...,2026`), not the stale promo_features parquet — new memory `promo-2026-data-from-tracker-pins.md`. Tracker data can't tell release from FA/trade → full 2026 release list needs the transaction pull. Promote = HOU-level-to-HOU-level only. Grades/levels must be at time of transaction.
