# 2026-09-01

## Magnet board batch + two access fail-opens (hiring/main)

13 commits, all pushed, Railway auto-deploys each one.

**Shipped:** multiple project boards; SP/RP replacing RHP/LHP; the field now
derives positions from the grid (it used to draw an empty park on a fresh
import); Potential Release / MNFA / Rule 5 boxes (Released / Traded on
current); UTIL beside the depth chart; board Delete behind a warning; sharing
no longer lists your own address; MNFA + Rule 5 year chips; calendar day-hover
notes panel.

**Three defects I shipped and then caught:**
- A fixed "dock" of the release boxes painted OVER the magnet being dragged.
  Measured it with `elementFromPoint` rather than guessing - the drag was never
  blocked, it was covered. Replaced with edge auto-scroll, which is what Zac
  asked for in the first place.
- The CSV importer built each player field by field and did not name
  `r5_first_year`/`mnfa_year`, so both were dropped on import. The query emitted
  them, the magnet asked for them, nothing joined the two.
- `milb_fa` was 0 for all 282 players - I shipped a chip that could never draw,
  which is the exact class blocking rule #19 exists to prevent. Zac's check
  query is the only reason we found out.

**Two access paths that failed OPEN:**
- `admins.role` DEFAULTs to `'admin'` and `upsert_admin` does not write it, so a
  coordinator whose level did not land became an ADMIN - more access than asked
  for. Now read back and disabled if it did not take.
- With no `RESEND_API_KEY` the mailer writes a file and returns ok:True. Fine
  for an invite; wrong for a first password, which exists nowhere else. The
  page said "added" and nobody could sign in.

**Zac said to remember:** he was worried this batch broke existing boards or
access. It did not, and that is checkable rather than a reassurance - only two
code paths write a board and both use the session's own identity.

**Still uncertain:** the Pot. MNFA 18-and-under term (+7) is fitted to Zac's
"one year early", not documented. Only Soto is verified against a GC2 card.

## Session - Aerollo: mentions, access model, program sub-pages (astroworld)

Long run on Astro World. PRs #46 through #60 all merged and deployed; #61
(em dash sweep) left open.

**Shipped:** absolute Central timestamps on comments; the activity rail on the
boards page (notes only, full text, `?card=` deep links); edit your own comment;
@mentions with an `AeroMention` table; the pre-launch access model (`SITE_LIVE`,
gates inheriting down the slug, green/grey edit chips on Manage access); program
sub-pages so `GC2 Onboarding ▾` can hold a Glossary; and Manage sections moved
into the admin bar because Zac went looking for it and could not find it.

**The bug worth remembering:** setting somebody to Viewer saved, displayed, and
then silently reverted on their next page load. Sign-in was force-promoting
anyone holding an Entra `admin` app role, and the Access screen cannot see
another person's app-role claim, so it showed the row as editable. Two rules for
one question and the invisible one won. Split into provisioning vs pinning.

**A claim I had to retract:** the storage banner told coaches their uploads would
not survive a deploy. Never measured. Zac's mp4 outlived a redeploy, so the
banner and its whole code path went.

**Zac said to remember:**
- No em dashes in anything he forwards. He had told me before and I ignored it
  all session. But the footer dash and the six domain blurbs STAY - they are
  written voice, not machine filler.
- Stop raising Aerollo card recovery and the dead supabaseStorage videos.
- Do not put pd-goals/EOY items in an Astro World status list.
- Do not rule options out on his behalf. I had written off SMTP because of a note
  in another repo; he wanted every route on the table for IT to choose from.

**My own pattern this session, three times:** a check that could not fail. A
case-sensitive match against CSS-uppercased text; a `/applied/` match against
prose containing that word; gate assertions routed through a function that
refuses for an unrelated reason. Each one hid a real defect until it was fixed.
Also learned the hard way that `tsc --noEmit` does not cover `scripts/` here but
`next build` does - that shipped a type error and cost two failed deploys, made
worse by me calling it a transient npm failure and telling him to re-run.

**Waiting on:** Peter/IT picking an email route for mentions. Six options written
up on the Desktop, nothing ruled out.
