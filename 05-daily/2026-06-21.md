---
created: '2026-06-21'
tags:
  - daily
---
# 2026-06-21

## For future Claude
Built the [[swing-path]] PDF page in [[hitter_analysis]] + full switch-hitter hand-split (vs RHP/vs LHP) across the report and doubled the app Swing Path by side; spent several rounds dialing the PDF 3D camera to angle "D" (elev 26 / azim −6, widened) — test target is [[Riley Unroe]] (gc 4773).

## Top 3 today
- New 3D **Swing Path PDF page** in [[hitter_analysis]] (2025 vs 2026 avg arc + metric table).
- **Switch-hitter hand-split** (vs RHP / vs LHP) across every visual page → 9-page report.
- Dial in the **PDF swing-path camera** to match the app look.

## Shipped
- **PDF Swing Path page** (new, [[barrelsville]] `feature/barrelsville`): 3D avg bat path 2025 (orange) vs 2026 (navy) + metric table — Bat Speed (at contact) / Swing Len / Path Tilt / AA@Contact / VBA@Contact / HBA@Contact / AA@Downswing / VBA@Downswing. Ball-Flight page moved to last.
- **Switch hitters ([[Riley Unroe]], bats S) hand-split everywhere:** P2 AA/LA 2×4 grid (xwOBAcon dropped — one-sided hitters KEEP it), P3 EV/Whiff 2×4, Swing/Contact 4 per-hand pages, Ball-Flight 2 rows (PoC/Spray) × 4 cols; dynamic page count = 9. One-sided RHH/LHH byte-identical besides the new page.
- Added `pitcher_throws` to `swing_path_data._SWINGS_SQL` for the hand split.
- **App** (Postgame Visuals → Swing Path): doubles by side for switch hitters — A/B/C buttons + metric tables rendered per side (vs RHP / vs LHP).
- **PDF camera = angle "D"**: `view_init(elev=26, azim=-6)` + X box-aspect widened ~2.2×, fan removed, home plate + strike zone + equal data aspect so the bat size matches the app; bat speed switched to AT CONTACT.

## Wins
- App swing-path doubling — Zac: "looks great."
- Angle D landed the bowl/U look (plate flat front-bottom, opposite side, elevated, wide).
- All page layouts render-verified on synthetic data before shipping.

## Friction
- PDF 3D camera took several rounds — my Plotly→matplotlib angle conversion was unreliable (kept missing 643's look).
- My rendered preview images weren't reaching Zac in the chat — had to save PNGs to `Downloads/` for him to open.
- Held a commit pending input instead of just pushing — corrected.

## Notes & ideas
- Converge on a 3D camera by rendering a **labeled angle sweep to a file the user can open**, and let them pick the cell — far faster than guessing.
- Camera tune point lives in `_draw_swing_path_panel` (`view_init` + `_wide[0]`).

## End-of-day reflection
Lots of churn on the camera; the fix was making the choice concrete (sweep grid → pick) instead of iterating blind. Core feature (9-page switch report + app doubling) is in and render-verified; real-data validation on [[Riley Unroe]] is the gate.

## Links
- [[swing-path]]
- [[hitter_analysis]]
- [[Riley Unroe]]
- [[barrelsville]]
