---
created: '2026-06-18'
tags:
  - daily
---
# 2026-06-18

## For future Claude
Morning: shipped switch-hitter 2-page fielder advance (Intangibles) and a [[hitter_analysis]] "Ball Flight" page, and a title-under-logo bug drove the new [[render-and-look]] BLOCKING rule. Evening: took [[indyball-tracker]] fully live on Posit — fixed the Pioneer datacenter-IP WAF block (laptop-scrape→pin, Connect reads), built the daily-3am Connect refresh notebook, and fixed the Pioneer pitcher scrape (`pos=p&sort=ip`, was returning 9 position players).

## Top 3 today
- Switch hitters → two spray pages (vs RHP / vs LHP) in the [[switch-hitter-advance-2page-2026-06-17|fielder advance]] (Intangibles)
- New "Ball Flight" page in [[hitter_analysis]] — Point of Contact + weekly spray, for [[Frey]] / [[Brutcher]]
- Stop shipping unseen visuals → [[render-and-look]] blocking rule
- (evening) [[indyball-tracker]] live on Connect with auto-refresh + Pioneer pitchers fixed

## Shipped
- **Switch-hitter fielder advance** (Intangibles, `feature/astros-intangibles`): `bats=='S'` now produces 2 stacked pages split on actual `pitcher_throws`; `get_hitter_page_specs` is the single source of truth across batch PDF / per-hitter PDF / app inline; hand-aware scope (4 per-hand buckets @ 80). Commits `79301c6e` → `efc3d4e7` → `e1a74891`.
- **Ball Flight page** ([[hitter_analysis]], `feature/barrelsville`): Page 6 = rows seasons × cols [Point of Contact freq | weekly spray + EV colorbar]; EV/Damage density dropped; spray reused verbatim; title seated in a tan box + `ylim` headroom so it clears centerfield. `acb6807e`, `0ac6816b`.
- `--no-heatmaps` condensed page now dual-season (`307fb2bb`); prior-season gating unified on `has_prev_zones` (`efbff3ce`); page-1 xwOBAcon zone value text −1pt (`aeecaccd`).
- **[[render-and-look]] BLOCKING rule #18** + `new-visual` skill Step 8 — synced all 4 worktrees.
- Live agent-memory updated + vault `/sync`'d (all touched files verified identical live↔vault).
- **[[indyball-tracker]] LIVE on Posit Connect** (app GUID `f37d76d5`). Standalone repo `zbridger_astros/indyball-tracker`.
  - **Merge-by-league refresh** — replaced the all-or-nothing pin write (a DNS-flake dead-end) with per-league scrape + merge; pin coverage is now monotonic, never loses a league. `write_pin` guarded so a Connect blip can't crash the job. Commit `4d1f880`.
  - **Pioneer pitcher fix** — was scraping the hitter URL for pitching → 9 position players; `pos=p&sort=ip` now returns the real ~125 pitchers. Commit `151e297`. Confirmed live after a laptop `refresh.py` rewrote the pin (892 pitching rows / 4 leagues).
  - **Connect-scheduled refresh** (`connect_refresh/indyball_refresh.ipynb` + `deploy.ps1` mirroring connect_pins_defense) — content ID 940, runs **daily 3am**, `attempts=5` since no 15-min re-tick; graceful no-op when `CONNECT_API_KEY` absent; per-league daily lock via pin-metadata freshness map.

## Wins
- The render-and-look rule paid off the *same session*: rendering + viewing caught the stray "P" under the logo and the spray title sitting on centerfield before either shipped further.
- Combined multi-player PDF already worked — multiple `--batter-ids` → one file.
- (evening) Proved the iScore 3 leagues auto-refresh on Connect (the bsb-resources connect-pin pattern) while Pioneer rides the laptop — both merge into the same pin and coordinate via a shared freshness map. Pioneer can now never go *missing*, only stale.

## Friction
- Shipped the switch-hitter title as a right-aligned tag that slid under the Astros logo (stray "P"); `py_compile` + unit test both passed, cost two round-trips. [[Zac Bridger|Zac]]: "these issues are killing time" → drove the new rule.
- Page-1-vs-rest season confusion on the no-heatmaps run ("confusing as fuck") — traced to the `--no-heatmaps` branch rendering current-season only.
- (evening) Felt like going in circles on IndyBall — every loop had a real cause though: work-laptop DNS flaking mid-scrape (wrote empty/partial pins), then nbconvert rejecting the hand-written notebook (`'outputs' is a required property`), then Connect executing the notebook at deploy *before* Vars were set. Each got a concrete fix.
- The "why isn't this the connect-pin ipynb pattern?" callout was fair — it IS for 3 of 4 leagues; only Pioneer's WAF forces the laptop. See [[datacenter-ip-waf-block]].

## Notes & ideas
- Switch hitters pull wider date ranges (hand-aware 80/bucket per side) — dial the per-hand floor down if windows come back too wide.
- Page-count vs render divergence (`pitch_df_prev` vs `level_rows_prev`) reconciled — zones/count gate on `has_prev_zones`, stat tables on `has_prev`.
- `mode="damage"` retained in `_draw_contact_point_heatmap` (no longer drawn, kept reusable).
- **Everything from the morning session is untested on the live DB** — work-laptop eyeball pending: barrelsville `--batter-ids 174203 130666` (no `--no-heatmaps`) + intangibles `--level dsl`.
- (IndyBall) Pioneer players page **hard-caps at ~125 rows** → we get top-125 by IP; sub-IP cameos beyond that are dropped. Acceptable for a stats tracker.
- (IndyBall) **2027 maintenance:** iScore season UUIDs in `config.py` change every season — re-pull from the SPA JS bundle. Pioneer keys off `SEASON_YEAR`.
- (IndyBall) Offered but not built: clickable per-player Pioneer League profile links in the app (slug already scraped).

## End-of-day reflection
The throughline was *look at the output*. Most of the morning's churn came from reasoning about coordinates instead of rendering them; that's now a rule, and it immediately caught two more layout bugs. The evening echoed it — the IndyBall loops resolved only once we stopped guessing and read the actual pin contents / scrape output / Connect logs. Diagnose from evidence, not theory.

## Links
- [[render-and-look]] · [[hitter_analysis]] · [[switch-hitter-advance-2page-2026-06-17]] · [[analysis-scripts-session-2026-06-10]]
- [[indyball-tracker]] · [[datacenter-ip-waf-block]] · [[merge-union-not-primary]] · [[tracker-pin-connect-deploy]] · [[indy-ball-scraper-status]]
