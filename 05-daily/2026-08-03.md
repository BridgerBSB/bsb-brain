# 2026-08-03

## Session - Draft-class handoff, Slack channels, acquisition-target scaffold (bsb-resources / feature/pd-goals)

Packaged the 2026 draft-class onboarding reports for Camden (23 PDFs + combined,
`pd-goals/reports/onboarding_2026_draft_class.zip`). The zip already on disk was
stale - built 03:21, before the 14:53 player-facing rewrite - so it was rebuilt
and 3 stale no-gcid duplicates excluded.

Slack channels: added z_ athlete ids to the existing zzz_ rows for Wesneski,
Teng, Abreu, Hader (`3ad5758f`), then found John Carver's row was named z_ with
an EMPTY channel_id, so his coach delivery had been going to overflow (`315a31a4`).
All 5 CSVs md5-identical, 4 worktrees pushed. Audit surfaced 11 more zzz_ rows
with empty channel_id, 3 orphan z_ rows (one is Radel, in the class just sent),
and 2 junk rows - all reported, none touched, waiting on ids from Zac.

Acquisition tool: Zac wants a `--deadline ORG` flow for the trade-deadline
channel (Aaron posts "BAL, Top 30", group replies with names + rationale).
Discovered acquisition mode ALREADY EXISTS in both analysis scripts - the only
gap was turning an org + tier into gc_ids. Shipped `find_acq_targets.py` +
`acq-org-prospect-targets.sql` (`51ef6c37`), UNRUN. Throw-In tiers blocked on
locating the GC2 TI tag table.

**Found and did not fix:** the onboarding page 2 Camden now holds is
parser-mangled - Butler's reads `1. 100th` / `2. CB Quality Spin Execution` /
`3. Percentile`, whiff+K numbers dropped. Only 2 of 23 checked. And the real
pitcher writeups turned out to be `Pitcher Development Handoff` .docx files in
Downloads, not the deck the parser reads - richer, and Butler's docx has four
goals where the PDF shows three. LINEAGE entry written.

**Zac said to remember:** the docx `Personal Background` sections are candid
about makeup and this report is player-facing - that content can never enter the
pipeline. Also: "ill test this a different day this is not really important at
the moment" re: the acq script.

**Process note:** I over-questioned the acquisition design - spent two rounds on
board-shape and gating options when the ask was simply "run hitter_analysis and
pitcher_analysis for the top 30." Zac had to pull me back with "or are you asking
about that???". Confirm the smallest reading of the ask before exploring options.
