# 2026-07-29

## Session — EOY position report + coordinator-notes app (feature/pd-goals)
- Wired position EOY into PD Engine app as coordinator-notes in-app submission (`f79d1c70`): eoy_notes.py stubs→real (PRP anti-wipe), 2 boxes only (Zac: no 3rd box, "tranquillo"), auto heading, PDF preview + Send, pin `eoy_notes_<season>`. Pitcher/ODP = placeholder (Camden).
- P13 OF/IF roses → colored vs **MLB pool** not highest level (`66e4e736`); caption "vs MLB" EN+ES.
- `%Hi` header → actual level `%A/%AA/%AAA/%Rok/%DSL/%MLB` report-wide (`935b7dde`) — removes coordinator confusion; %MLB unchanged.
- All UNVERIFIED vs DB/Connect (personal laptop). Next: work-laptop redeploy + 1 test send; keep editing report + notes app.

## Session — IF/OF Range & Difficulty (intangibles, feature/astros-intangibles)

21 commits `5e1f7241..7608c5fc`. Zac deployed `3788a839`: "this looks great at the moment".

**Three layered root causes, each only visible after fixing the one before:**
1. **Coordinates** — IF spray plotted the ball's FIRST BOUNCE (~15 ft off the plate on a chopper). Now anchored at the point on the ball's path closest to the fielder's start.
2. **Population** — gated on `first_defender`, which means "who first TOUCHED the ball". On a ball through the 6-hole that is the LEFT FIELDER, so every ball past the infielder was deleted — the plays that define range. Now a user toggle.
3. **Labelling** — `dcbp.out_made` is per-FIELDER, so outs another fielder converted rendered grey. Now `event_result_id` (official scoring), three classes incl. Error.

**The defect shape, worth remembering:** every single bug was *one fact rendered in several places, only one updated*. Fill without label. IF without OF. Toggle-off without the default. App without PDF. Anchor without hull. Zac caught each one. Countermeasure now in code: a per-play assertion that app fill == app hover label == PDF fill.

**Zac said:**
- "since when has a fielders choice been an error???" — correct, it is an OUT (defense chose to retire another runner). At 22k league-wide it was the biggest false-purple source.
- An OF who fields a single and throws a runner out at third "should read Hit" — i.e. the label is the BATTER's outcome. That killed the `pitch_result_id` proxy.
- "you have made me run in circles multiple times" — fair. Fixing one surface at a time was the cause.

**Council/verified dead ends (do not retry):** `Play_Event_Positions` event 17 and OPIDA `throw_xf/yf` as the spray anchor — both exist only on plays the fielder COMPLETED, so anchoring on them deletes the balls that got past him and flatters the worst-range fielders most.

**Ended on:** performance, not correctness. Initial load heavy, everything after fast. Cause = 11 separate `Astros.Video_Network` scans (one per camera angle) + OF's 400k league band read. Next step is the join collapse, built as a diff harness.
