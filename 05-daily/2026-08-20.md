# 2026-08-20

## Injury Tracker - level picker, 2 club analyses, PDF (session 411a)

`bsb-resources` / `feature/pd-goals`. Four commits, all pushed:
`fb198582` MiLB level picker - `3565d92b` lower-body share + 60-day reinjury -
`95a69646` 2-page landscape PDF - `ac2d4b05` retitle.

**What Zac asked for, in order:** a way to pick specific levels when MiLB is
selected "without making it look retarded"; then two queries (lower-body injury
share vs the other 29 clubs, ROK-AAA; and 60-day IL reinjury); then one-off
landscape PDFs with a column key on the right.

**Decisions he made:**
- Level picker: he passed on the plain single-multiselect option ("this tickles
  my fancy more than 1 even tho 1 seems more plausible") - built the popover.
- Narrowing to specific levels DROPS blank-level stints: *"filter and be
  reactive to level!"*
- Q1 wants BOTH percentages (stints and days) each with a rank: *"4 instead of
  2 columns ... even tho i lean stints - good to have both sets."*
- Q2 reinjury = *"the days missed during stint #2 or 3 ... whatever occurrences
  happen when the injury recurs after they have played games."*
- Ranked per-return, ROK-AAA, returns only.
- Killed my page-2 title ("What a Club Gives Back After a Return") on sight -
  *"what the fuck does that mean?????"* Now `60-Day IL: Reinjured Days`.

**Two real bugs the render caught, neither visible to tests.** The key panel had
a fixed height and page 2 has one more note than page 1, so its last line drew
below its own box. And zero-return clubs showed `--` on GREEN - a club with no
60-day returns has `post_days = 0`, which ranked it FIRST on the raw total.
Blocking rule #19 twice in one page.

**Two of three layout injections went green first** - each a defect in the check,
not a pass. A green injection is a finding about your check.

**Closed with:** *"we are good on the injury stuff at the moment and will come
back later with more"* - then asked to clean up documentation loose ends and
move to other projects. The standing loose end is the rsconnect `--app-id`
lesson, still not in `.claude/rules/`.

## Session - IT-facing DB documentation (afternoon)

IT asked for documentation of what runs slowly on Posit and why we pin
everything. Read the pin/tracker code across all four worktrees and shipped
two handoff docs, MD + PDF, on `feature/pd-goals`.

- **Doc 1** `2026-08-20-sql-server-cost-inventory-for-cube` (16pp) - ten
  operation shapes, 18 `path:line` citations, 15 verbatim code blocks. The
  "why is it slow" half.
- **Doc 2** `2026-08-20-scheduled-workload-inventory-for-it` (7pp) - 12
  recurring jobs, 12h cadence, 16 bundles, replacement object per job.
- Renderer `_render_cost_inventory_pdf.py` (markdown -> HTML -> headless Edge).

**Zac supplied the headline number** - a screenshot of the postgame card's own
load-time panel: 150.33s cold build, `get_level_percentiles` **146.13s =
97.22%**. Everything that is not percentiles totals under 3s. That one image
does more work than three pages of my prose did.

**Two corrections from Zac, both about audience:**
1. I opened doc 1 by comparing it to the July R&D inventory - which IT has
   never seen. Cut entirely. Section 7 was also titled "What each side owes
   the other," which reads like our ask when they were the ones asking.
2. On the companion: *"we shoudlnt referneec any peoipkle ... josephy left teh
   comapnyt ... im not telling u any more names cuz u randomly refernece them."*
   Checked first - the July doc names a person, a vendor, and another team in
   its first 60 lines. Wrote doc 2 fresh rather than scrubbing. Also found
   `CLAUDE.md` still listed the departed IT contact by name and email; replaced
   with an instruction not to put a name there at all.

**Remember:** no names but Zac in any outgoing artifact, sweep before every
handoff. The July doc is internal only.

Next: port `AC_TIMING` to the other three apps, run each pin once with it on,
turn the DERIVED tags into MEASURED. Docs are NOT yet sent to IT.
