# 2026-08-19

## Injury Tracker - back up, then the punch list (session 9760)

Started as "the app says No IL stints derived for 2026". Ended up being that
**there are two Injury Tracker apps in Connect**: `deploy.ps1` deployed by
`--title` with no `--app-id`, and title does not identify existing content -
rsconnect matches on a machine-local record that is not in the repo. So a
deploy from another clone created a second app with an empty Vars tab, which
presented as an SSPI/Kerberos DB error. The app never lost its variables; Zac
was looking at a different app.

Shipped: `e48813b3` `0bbba2ef` `cb368aee` `3f74e023` `21efd04a` + a LINEAGE entry.

Zac ran `/orchestrate`. Five parallel read-only scouts (implementation agents
would have collided on one file). **Two of them corrected me** - the
zero-duration timeline bar is not dropped, it renders as a blank line, and the
thing that actually removes a player is a y-label collision plus
`barmode="overlay"`. And the level label had a second definition in a SQL CASE.

Three of seven fail-on-purpose guard injections came back GREEN, each a real
hole in the guard. Render-and-look caught a duplicated-integer y-axis that no
test could see, and disproved a scout's prediction about the level bar.

**Decisions:** `'r'` reads **ROK**, not FCL (it is rookie/complex for all 30
orgs, so it covers Arizona Complex clubs). Position type becomes a segmented
control matching its neighbours. Empty selection means ALL on both filters,
never an empty page. Tables default to most recent injury.

Zac: "beautiful this is really good where its at at the moment" - paused
mid-flow, "we have a few things to look at here" next session.
