# 2026-07-10

## PD-Goals — remaining goal-metric wiring (from affiliate trackers)

Wired the last un-wired PD-Goals goal metrics, pulling every formula from the
affiliate trackers as source of truth (5 parallel extraction agents). Shipped +
pushed to `feature/pd-goals`:

- CU IVB/HB compound shape (reword `Avg CU IVB below -8 and HB above 8`).
- Breaking-ball InZ% + BB Pre-2K InZ% + overall Pre-2K InZ% (fixes the dangerous
  silent-wrong `breaking balls 0-1inz%` that scored vs generic InZ%).
- RelX + RelH, Attack Angle (SCV), Arm angle (Brodie) + new `Direction.RANGE`
  two-sided grammar, PL1B (rides in get_hitter_stats like SB).

Ran a high-effort workflow code-review — it caught a **crash I introduced**: a
`target2` replace-all clipped `get_compliance_emoji`, which would TypeError the
goal card on every goal. Fixed that + 4 logic bugs (arm-angle range over-fire,
target2=0 falsy collapse, BB Pre-2K alias gap). All golden-diffed 0.

### Decisions / to remember (Zac)
- Rewords locked: `Avg CU IVB below -8 and HB above 8`; `Increase FC Usage to 20%
  in Pre2K vs RHH` (`<`=floor→increase); `Increase CH Usage to __% vs RHH`.
- RelX and mechanical metrics are operator-driven (the goal's `< / >` decides
  met/unmet), NOT a fixed higher/lower-better.
- Compliance auto-refreshes every 6h via the `connect_pins_compliance/` notebook,
  but that scheduled job needs the bundle REDEPLOYED to run this session's new
  code — two deploys (app + compliance bundle) for this change.

### Still open
- 6 new metrics DB-unverified (personal laptop has no DB) — work-laptop verify next.
- Deferred: percentile-color pools + grey sample-size text for the new metrics.

## Session (late) — college-reliever dig
Built Sam college-relievers->MLB-relievers dig: colored PDF (generate_college_reliever_dig.py) + 2 SQL files. Traditional stats from box (College.YTD/gamelog), stuff tracked, colored vs 2026 MLB RP, real College.YTD gs/gp gate. Many blind-SQL error round-trips (signed=Y varchar, POWER wrap, SET-in-subquery, samp_outs). All on feature/pd-goals, latest eb156df6. Also diagnosed Ricky AA IF PAA/EO (not a bug). Next: work-laptop run.

## Session — PD Goals usage/shape N/A fix + two-sided platoon goals (~16:40)
- **Root cause (Aubrey "CH Usg vs RHH" N/A everywhere):** parser metric-resolution, NOT a season bug. App re-parses goal text live; "Usg" != "Usage" -> raw-text fallback -> N/A in all 4 timeframes. All 4 timeframes route through one value fn, so a resolved metric colors in all of them.
- **Shipped (origin/feature/pd-goals):** `e181f2f5` parser (Usg->Usage + two-sided platoon2 grammar + all-PT shape regex) + stats.py all-PT IVB/HB + display wiring (app/report/compliance fetch metric2 under platoon2) + spec doc. `928052bf` fix_goal_texts.py (safe read-modify-write pin edit, no wipe). Parser verified offline 14/14 incl. arm-angle RANGE untouched.
- **Decisions:** two-sided usage = one goal, two hand floors (metric2+platoon2), RHH primary. Weber CU shape rewritten to IVB -8 / HB 8 (both `above` — flip if "more break"). Did NOT edit goals.csv (stale/pin-authoritative; re-pin would clobber PRP goals) — targeted pin script instead.
- **Remember:** goals are PIN-first (PRP Send writes pin; goals.csv is fallback + stale). Concurrent amateur/dig session also live on this branch (81 uncommitted, not mine).
