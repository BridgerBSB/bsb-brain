# 2026-07-15

## Session — PD Engine: PRP corrections, velo-threshold automation, transition 3rd channel

**Worked on (all `feature/pd-goals` + siblings, pushed):**
- **PRP no-roll correction process** — built `redeliver_prp.py` (edit PRP pin + rebuild PDF + optional deliver, never `apply_prp_goals`) and `edit_goal_row.py` (surgical in-place edit of the live `pd_goals_data` current goal row, strict pin read that aborts rather than clobbering the stale bundled CSV). Ran live for Trevor Austin (React .475→.500), Yamal Encarnacion (steal 40→50), Hector Salas (steal 40→20), Huezo (goal 3: clear Why + set How "Get familiar and work with your staff in A+"). Worked. Documented as rule `prp-correction-resend.md`, propagated to all 4 worktrees.
- **Velo-threshold automation** — added `--deliver` to `generate_velo_ceiling_heatmap.py` (the FF/FT Velo Ceiling by Zone batch = the "hitter velocity threshold" report Caufield/Hennigan asked for) → #velo-threshold (C0BHJ9CM2EQ). Wired a `velo-thresh` step into `run_monday.ps1` (Stage 1, all 4 lockstep sections, PS parse verified).
- **Transition 3rd channel** — `resolve_channels` now always CCs #transition-reports (C0BHHDWC85Q) on real submits, alongside coach + affiliate. Verified end-to-end through the submit handler.

**Decisions / to remember (Zac):**
- Once a player has been through a PRP Send, their goal phases live only in the live pin — `goals.csv` is stale, so `/repin-goals` would wipe later phases. Goal-number changes for those players MUST use `edit_goal_row.py` (surgical), NOT the old CSV-repin flow.
- Zac prefers decisiveness over clarifying questions when the info is derivable ("you're supposed to know"). Reserve questions for genuinely ambiguous / his-knowledge-only forks (e.g. which existing report he means).
- Reports were hand-posted (`--no-deliver`) rather than auto-delivered for the PRP corrections, per Zac.

**Pending (Zac, work laptop):** git pull hitting+resources; add "Astros File Uploader" to #velo-threshold + #transition-reports; redeploy PD Engine for the transition routing.

## Opportunities Bot — defense expansion D1+D2 (built + live-verified)
- Audited the whole bot (offense/defense/pitching metric inventory). BUILT D1 fielding levers (arm/react/react-rad/usereact/accel/topspd/exchange, position + directional) + D2 catching (NetK/framing/blocking-SurPP/pop/augpop/arm/exch) via subagent-driven-dev. Commits fc506f67..b4e824de on feature/pd-goals.
- Live run on work laptop WORKED: defense_signals_2026_2026-07-15.json — 573 signals, 57 directional, 24 catcher. Reformatted 5-per-side goals table in chat.
- Zac revisions (next = /plan, in spec REVISIONS b4e824de): 4 goal categories (Offense/Baserunning/Fielding/Catching, 5 each); fielding gate >30, OF arm single not per-pos, fielding LEVEL-AGNOSTIC one pool; catching own category + expand (Bounce%/Pop3B/Depth/CS%); offense drop redundant Whiff-or-Contact + remove TopSpd + rework hitting level tag; baserunning own category. A/B display CODE fixes first.
- Remember: the defensive levers ARE goals; C = use DSL data as-is (don't strike "artifacts").
