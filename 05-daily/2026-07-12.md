---
tags:
  - daily
created: 2026-07-12
---
# 2026-07-12

## For future Claude
Shipped the optional Opposing Org Observation section on the Coordinator Notes app end to end, then refreshed the app's Obsidian docs.

## Shipped
- Coordinator Notes: optional "Opposing Org Observation" section (org dropdown from new `mlb_orgs` table defaulting to N/A, plus how-the-org-operates and opposing-staff-standout text). Committed `1283c40`, pushed, Railway-deployed. Supabase migrations applied to prod (mlb_orgs + 3 `visits` columns). Wired into the submission email and RAG (`opp_org_observation` chunk). Section is optional: excluded from the progress bar and the min-answers submit check.
- Reset Zac's Supabase Auth password to the default `AstrosPD2026!` (bcrypt via pgcrypto) after login started failing.
- Vault docs refreshed: `memory/coordinator-notes-app.md` (PLANNING to PRODUCTION), new `projects/coordinator-notes-questionnaire.md` + `projects/coordinator-notes-rag.md`, and a Standalone-apps cluster in `[[MOC-astros-engineering]]`.

## Decisions
- Org list lives in a Supabase table (`mlb_orgs`), not a hardcoded constant, so the RAG tool can query it like the coordinator roster.
- One opposing org per visit (3 columns on `visits`), not a child table.
- Standing rule captured: NO EM DASHES EVER. Repo-wide cleanup of existing em dashes deferred; do not introduce new ones.

## Friction
- The documented default login password had drifted from what was actually set in Supabase, so a manual bcrypt reset was needed.

## Notes & ideas
- Deferred: repo-wide em-dash cleanup + add the rule to the repo CLAUDE.md.
- Dangling vault link `[[player-evaluator]]` (real basename `Player-Evaluator-Agent`) to repoint or alias.

## Links
- [[coordinator-notes-app]] · [[coordinator-notes-questionnaire]] · [[coordinator-notes-rag]]


## promo-engine session (gate cleanup + research + point-score + YoY)
- Board gating fixed: 60 BF unify, thin players blank (grades + percentiles), PA/IP shows, no diamond. Caught a pandas nullable-Int64 .rank() trap (phantom 16/14 percentiles) via render + local repro.
- IP/PA gate research DONE + rigorous (split-half self-test passes): gcOBA ~286 PA, K-BB% ~126-162 BF; stuff/velo <100. 50/60 is a floor, not a reliability point. Gate CHANGE deferred (Zac call, last).
- Point-score built in the drawer: each driver wt/push/pts, component totals, vs-avg anchor, explainer by donut. App-only. Zac loves it.
- YoY offseason carry shipped + PROVEN (--year 2027 -> carries 2026). MODEL_NUMBERS.md written (legs 71/2/27, NOT 70/12/45).
- Remember: gate change is LAST + Zac's call; per-player prior-season reach-back parked (Sam may request); keep PUSH, PTS reads last.
