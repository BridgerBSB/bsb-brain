# 2026-07-06

## Promo-engine drawer + gate marathon (continues 07-05 evening)
Long session on the Promotion/Release app. Shipped 10 commits on `feature/promotion-models` (`95b8ea74`→`a3e8bca0`):
- **Drawer redesign** shipped: rendition-C twin-donut, all 7 features under BOTH release ponies (No-Future + Cut-Risk) as two-track WT/PUSH bars, promote 3-way donut + Current/Age/Level tiles. Then added the **promote two-track** bars (Zac kept asking) via `projection_driver_detail`.
- **CUR_BUMP 2.0→1.0** (pool by sample; council-DS + OOF sweep both said the bump carries no signal).
- **Caldera bug**: promote HARD-DROPPED sub-floor players. Fixed to show-with-**None** (Zac: "put None", not an estimate). Pitcher floor **60→45 BF**. Insufficient badge.
- **K% wt-0%** fix for FCL/DSL pitchers (complex band uses K% not K-BB%).
- `score_all.py` = one command runs both scorers.

**Zac decisions to remember:** insufficient players → NO grade (None), still shown, in both promote AND release. 45 BF is the realistic pitcher floor. No em-dashes in outputs. Promote should mirror release (two-track). Models are frozen (deterministic refit on frozen parquets). Cross-season = most-recent (NOT pooled across years).

**Big find:** career-MLB stats (`career_mlb_pa/bf/war`, grad-pctiles) are ALREADY in the feature ETL output — the MLB-into-release discount needs NO new work-laptop pull. That's the next build after /clear.

**Next:** MLB discount (Option A serve-rule) → then YoY offseason carry → release PDF. Zac to run the 4-line re-pin/redeploy to make tonight live.
