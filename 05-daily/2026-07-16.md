# 2026-07-16


## Session — Usage-table PR review + hitter_analysis fixes
- Reviewed cquick's merged PR #11 (Arm Farm Usage-by-Count table). Fixed: xSLG was a flat prob-weighted base sum → swapped to canonical GC2 xSLG (exponents + out-normalization, `generate_slg_vs_xslg_heatmap.py` / GC2 Event query); aliased canonical `PITCH_TYPE_COLORS`; fixed `KeyError 'pitcher_id'` (→ `int(player["groundcontrol_id"])`) that crashed the whole PDF run. gcPerf/Whiff%/IZ% were already correct. Commits `c794b048`, `28625e65`.
- Barrelsville `hitter_analysis.py`: single-batter runs were building all 7 league percentile pools. Scoped to the batter's actual levels (`34ada198`), then fixed the probe to span BOTH report years (`c2398f4f`). Bat speed on the swing-path page didn't match page-1 KPI — swing-path `metric_table` took a raw mean; routed through `clean_bat_speed_per_player` (`2b81ec9f`), which also aligns the shared app Swing Path visual.
- **Decision (Zac):** wants per-`(level, year)` percentile pools so a 2025 line ranks against the 2025 league, not 2026. Currently only current-season pools are built and prior-year rows borrow them. Values are already year-correct (league_envs both years); only the coloring is wrong. Next session: implement in hitter_analysis + mirror in pitcher_analysis. This is the "fire away" item after /clear + /brief.
- Deploy note: apps auto-deploy off feature branches (corrected my earlier wrong assumption that nothing deploys off-branch).


## Session 2 — Camden PR review + merge-regression fixes
- Reviewed Camden's two merged PRs. **#12** (Arm Farm Usage-by-Count restyle: pitch-color parent bands + bordered diverging value chips) — the restyle merge landed on top of our 7/14-7/16 work and clobbered it: stripped the `exps` param + `build_usage_count_matrix(..., exps=exps)` (reverting our canonical-GC2-xSLG plumbing `c794b048`) AND left a `player_levels` reference whose def was dropped. Two live crashes on origin: `player_levels` NameError (fires first) + `exps` TypeError. Fixed both — `84b8f7a6` (re-thread exps → restores canonical xSLG + kills TypeError) and `e3302c7a` (derive `_uc_levels` from `level_rows`+`label_to_code`). Camden's restyle fully preserved; our year-correct pools + pitcher-id fix + all hitter_analysis work survived.
- **#13** (BR 1→3/2→H 3rd-out-on-the-play wash credit) reviewed — solid: new BLOCKING rule `br-advance-3rd-out-wash.md`, shared `baserunning_base.py` credit helper (success+1 always, opp+1 only ft3+LF, per-(runner,org) keying), `br_advance_credits.csv` (4 seeded), three-surface parity handled (tracker/KPI-table/postgame; chart deferred, PD-Goals N/A), deploy bundle wired. Merged by Zac.
- Separately: dropped affiliate-channel (z1-z4) delivery from Transition reports — `fc4ff735`, keeps zzz_ coach + #transition-reports only.
- **Lesson (flagged):** `py_compile` can't see NameError/TypeError inside a runtime `if` branch. After a teammate's restyle-PR merges into a file we recently changed, DIFF the merge vs our last commit + trace/run the touched block — don't trust compile. Two crashes shipped to origin exactly because the merge silently dropped a referenced-but-redefined var and reverted our plumbing.
- **Framing (Zac):** this is done/shipped work, NOT an open project — Arm Farm pitcher_analysis is a living tool he'll stop by and tweak.

## Session (late) — Fielder Progression Report (intangibles)
Built the Fielder Progression Report end-to-end (OF/IF/Catcher): weekly multi-player
matrix PDF, one metric/landscape page, level-stacked AAA→DSL, month-by-month percentile
colored + L6W/Year/ΔMoM/YTD/YoY. Ran adversarial 4-lens review (caught a Cum-pool blocker
+ empty crash + YTD-April + non-fielder rows, all fixed). Late fixes from Zac's eyes:
real names (Astros.Players, was gcid), Cum→Year, MLB games included for currently-MiLB
players (Whitcomb rule, _DATA_LEVELS), and additive-metric Year=sum-of-months
(NetK/OAA/BlockRAA/SurPP undercounted b/c get_catcher_leaderboard returns per-LEVEL rows).
Decisions: pins OK for now (canonical set hits pins); MAYBE switch to live SQL later.
NEXT: /code-review the 3 progression modules, then maybe pins→live. All pushed (02119671).
Zac remember: NetK/OAA are additive → Year must = sum of months = tracker season total.

## Session — EOY Position-Player report Pages 1-4 (feature/pd-goals)
Kicked off the EOY Position-Player report (HITTING half, 12 pages) via `/goal`. Saved the
spec verbatim + the 3 reference PDFs (McFeely/Brodsjo/Mejia) + 6 images into
`pd-goals/docs/plans/mocks/` (durable; Zac was annoyed they were never saved before). Crafted a
<4000-char `/goal` condition. Built `pd-goals/src/eoy_position_report.py` (data layer STUB —
no DB on personal laptop; synthetic render-and-look harness renders a PNG per page).
- **P1** Season Overview (autofit name, Season Summary panel, affiliate logos) `fa6051f9`
- **P2** Overall Season Review (plottable results table, grouped Total│levels│hand│months dark
  separators, 3 xwOBAcon 13-square grids) `0bb84a82`
- **P3** Batted Ball Spray (Daikin fence spray + EV dots + HHB metric table) — 4 tweak rounds
  from Zac: %ile under High-Lvl+MLB, horizontal EV bar + bigger spray, then avg + ▲top for ALL
  3 cols, then Player=avg only + explanation up top. Final `4ed9a6c7`+.
- **P4** EV by launch-angle + spray-angle line charts (blue=max/red=avg, home-plate marker at
  −5/5° bin) + fuller Driveline-style sub-captions `c3d6086c`/`00c9de33`.
- Build log + all locked decisions documented in the spec `048a508b`.
- **Decisions locked (Zac):** P6 grades = Barrel%(power)/Contact%(contact)/SwDec, %ile→20-80.
  Multi-level Total-row %ile pools RAW OBS across levels (blocking #16). P3 table format + P4
  chart format locked (in spec build-log).
- **NEXT:** /clear → /brief → **Page 5 (clean distributions)**, then P6-12 same fashion. Real
  data + per-cell %ile coloring wire on the WORK LAPTOP.
- Zac: "blast through 5-12 in the same exact fashion and document that shit."


## EOY Position-Player report — Pages 5-9 (late session)

Continued the EOY hitting report build (`pd-goals/src/eoy_position_report.py`, `feature/pd-goals`, synthetic render-and-look). Shipped P5 (stacked ridgeline distributions w/ pool-mean numbers, Brodsjo p3-4), P6 (McFeely internal-grade splits heatmaps — overlay reworked to the swing-decision damage-field style: RdBu_r + transparent-where-sparse + **pixelated `nearest` blocky cells** per Zac), P7 (Swing Location Density, FB/OS/BRK KDE like hitter_analysis p4), P8 (Pitches-faced heatmap + BIPs scatter, ONE img745 page), P9 (LA×Spray + POC×Spray line charts, IMG_8502).

**Zac caught several documentation lost-in-translations** (page ordering): img745 = single 2-panel page (pitches+BIPs share one page, not two); swing density is its own page (→ P7); old "Page 9 [img745]" double-counted img745 (dropped); spec was missing IMG_8501 (POC×LA + EV×POC) entirely. Back-half remapped: P9 = IMG_8502, P10 = IMG_8501, P11 = POC overhead+side (img747), P12 = glossary.

**Remember:** P6 grade overlay must be pixelated/blocky like the SwDec batch (not smooth). P6 flagged for real-data refine + review (verification-backlog). Next = P10 (IMG_8501: POC×LA lines + EV×POC regression scatter w/ CI band — new chart type). Commits `d9383e93`→`37632a46`.
