# 2026-08-16

## Session - EOY goals correctness + two pins nobody read

Fixed the EOY goal bugs Zac flagged. Range goals said "Below Target" on a value
ABOVE the window (Rodriguez 34.1 vs 27-32) - words and delta now measure from the
edge actually missed. Zac confirmed the goal corrections look right on both pitcher
and position decks.

**The loop we finally broke:** "we fixed it yesterday and it's back today" was the
scheduled pin jobs. `connect_pins_compliance` and `connect_pins_eoy` are separate
deploy targets from the apps and ship their own copies of `compliance.py` +
`goal_parser.py`. Neither had been redeployed, so every scheduled run rebuilt the
pin with pre-fix code. FOUR deploy targets, not two.

**Two pins written and never read.** The pitcher payload pin had been pinning since
the day it was built with nothing opening it - the position deck read its own pin all
along, and that asymmetry was the entire load-time gap. And `_EOY_LEVELS` was
hand-maintained beside `eoy_roster_select.LEVELS`, had drifted three times, and was
costing 27s of a 47.5s build on every MLB deck. Now derived.

**Zac's fair criticism, worth keeping:** he raised slow loads three times before I
asked for a Connect log. One paste located 26.9 seconds inside a 47.5-second build
and named the two exact pool keys - more than several turns of theorising produced.
Ask for the log first.

**Also mine:** I added a module-scope import without shipping the module in the pin
bundle, which failed the deploy - the same class of mistake I'd caught and written a
commit message about an hour earlier. `audit_pin_deploy.py` catches it and I hadn't
run it.

**Decided:** shorthand shape goals (`Develop FT 10 IVB x 15 HB`) can't state
direction, so Perez's Phase 2 was rewritten to explicit form. Zac's insight: IVB is a
ceiling for every pitch type except FF, and HB direction falls out of the sign of the
target. Proposal pending his answer on FC.
