# 2026-08-30

## Aerollo (Astro World) - Kanban shipped, then rearranged from Zac's Trello recording

- **PR #44 MERGED and LIVE.** Zac merged and ran Admin > Database. Aerollo is our
  Trello inside Astro World, top bar right of ManagerHUB, **open to anyone who signs
  in** (the gate is `getSessionUser`, deliberately not a role check - do not
  "restore" one).
- **PR #45 opened** from Zac's 160s recording of a real Trello board: work in the
  MIDDLE (description + attachments), conversation on the RIGHT. Move/Copy/Archive/
  Delete moved into the card's `...` menu. Real file attachments - images and
  **video that plays inline in the chat**, not just links.
- **Zac said to remember:** column names are editable and we are building from
  scratch, so matching Sam's old Trello board exactly is off the table. His board is
  private (401) and the Chrome extension would not connect.
- **Sam Niedorf (Slack) asked for two things.** Upload video/photo -> done in #45.
  A **date/time stamp on every comment so they can track it** -> NOT done, and Zac
  has already replied that it is coming. That is the next PR.
- **Watch out:** `STORAGE_DRIVER=local` in production means uploaded videos do not
  survive a deploy. The UI warns, but the real fix is `azureBlob` and needs IT.
- Three lessons re-learned the hard way: a heredoc eats backslashes (broke a test
  twice), `next dev` first-hit route compilation causes phantom test failures - use
  `next build && next start`, and an apparent "auto-scroll is broken" was really the
  test grabbing a card clipped out of view.
