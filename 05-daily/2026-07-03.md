---
created: {{date}}
tags: [daily]
---
# {{date}}

## For future Claude
<!-- one line: what happened today and why it matters -->

## Top 3 today

## Shipped

## Wins

## Friction

## Notes & ideas

## End-of-day reflection

## Links
- [[ ]]

## Session ~20:09 — one-off asks (SQL + slack)
- AAA bullpen: two report shapes — gamelog real ERA (`sql-queries/aaa-bullpen-vs-30-clubs-2026.sql`) and our KPIs (FPinZ/0-1 IZ/InZ/R2K/K-BB/Whiff/gcERA/gcPerf ranked vs 30 clubs). Reliever = "didn't throw the game's inning-1 first pitch" (per-appearance, no is_start flag). gcERA is NOT real ERA — relabel era→gcera before Sam sees it.
- Low-velo college RHP query: `<90` avg FF in final amateur (bbc) season, drafted 2018+, RHP via throws='R'. **Doc trap: school_type 4-yr code is `'4YR'` not `'4Y'`** (rule docs wrong; power5 script confirms). Added amateur→2A+→all-pro FF velo + delta, sorted by velo gain.
- Daily reports: `run_daily.ps1` = whole cascade; individual scripts run standalone (e.g. `intangibles/scripts/generate_of_report.py --date <d> --deliver`). `--deliver`=coach, `--deliver --z-channel`=athlete.
- Slack: added Axel Delira athlete channel `C0BAMEEA8S2` to z_channel_id of his existing row (SAME row as coach — Zac corrected me, correctly). Pushed all 4 worktrees (`e5d52df4`).
- **Remember:** coach + athlete Slack channels live in the SAME CSV row (channel_id vs z_channel_id); deliver.py is last-row-wins per gc_id so a duplicate row breaks routing.

## Session ~22:00 — Command CV video pipeline unlocked + training-set puller
- Recovered post-crash: Command CV command-tracker. Locked the contact decision — Model A (Trackman plate_x/z = actual, ground truth) vs Model B (CV setup-glove), calibrate B to A via zone homography so no-Trackman pitches work.
- BREAKTHROUGH: Zac's Okta login unlocked the MLB Stats API. Two-step auth pinned: Bearer for `analytics(video)` endpoint, browser-UA (Cloudflare) for the sporty-clips .mp4 CDN. Verified end-to-end.
- Built `pull_pitcher_clips.py` — season gameLog → clips + ground-truth manifest (plate_x/z from the authed analytics payload since A+ feed has none). Pulled Ogando 50/50, Langford 30/50 (1hr token expired mid-run; idempotent resume).
- Key finding: A+ (Asheville) = one BROADCAST angle only (behind-pitcher, mitt visible), no CENTERFIELD feed. And the SETUP glove is the ONLY thing no API returns → Command CV's edge. batTracking.impact.contactPoint may give contact-pitch actual as data.
- Decision (Zac): bootstrap subset (~50/pitcher) → annotate → train YOLO → auto-apply to all 1,108 → audit later.
- Housekeeping: MEMORY.md compacted 45KB→15KB (was truncating at load).
- Remember: Okta token expires hourly — ask Adam for a service token.
