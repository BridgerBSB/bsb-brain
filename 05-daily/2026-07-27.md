# 2026-07-27

## EOY report — P20 throwing + P21 base-running revamp (feature/pd-goals)

Reworked two EOY position-report pages from Zac's chat feedback. 4 commits `9ac5c677` → `27787d09`, all pushed, render-verified synthetic (fixed-page + tight-bbox), UNRUN vs DB.

- **P20 Throwing → density.** Replaced the AugPop-colored Nadaraya-Watson surface with a throw-arrival 2D KDE (white→navy density + dots). AugPop moved to the table only. Zac confirmed it's the player's OWN throws (not league-relative); offered a `contourf` filled-band variant, he was "just curious" — not decided.
- **P21 Base Running.** SB count now graded like everything else (new BR `(SB count, season, total)` golden-gate tuple, 30-on-base gate). Dropped "(90 ft)". Removed the on-path lead band → new per-base **lead-distribution panels**: primary lead split vs LHP (orange) / vs RHP (blue) with dashed league-average-by-hand. Green TL label brought level with blue PL.
- **Footnote overflow** on both pages was running off the page edges — wrapped to 2 lines inside the page; raised the P20 density chart so the caption clears its x-axis label. Made the distribution panels self-descriptive (subtitle + x-label + "league avg" legend).

**Zac reactions:** "looks fantastic" on the BR page. Confirmed it runs on real DB (his screenshot showed real values TL 19.2/24.0). Briefly got frustrated thinking the push failed — it hadn't; the "Changes must be made through a pull request" line is just a branch-protection reminder, the push went through and a later commit (`7fa6febb` opportunities bat-speed) had landed on top.

**Remember / next:** Zac /wrapped saying **"decently hefty changes to make to the report and chat about"** next session — he hasn't named them yet, so ASK first. Work-laptop verify + metric-audit the new SB-count percentile + by-hand PL leads vs the intangibles BR tracker still pending. Dead `_LEAD_SAMPLES_QUERY` left in `eoy_br_data.py:122` — delete on next cleanup.

## Session (cc96) — Barrelsville hitter_analysis Swing-by-Count + Zone-Production pages
- Built 2 new per-player pages in `barrelsville/scripts/hitter_analysis.py`: Page A "Swing Decisions by Count" (ALL/FB/SEC × count buckets) + Page B "Zone Production Map" (region tables + zone-heat). feature/barrelsville HEAD 8fd7edfe, all pushed, render-verified SYNTHETIC only.
- League percentile coloring: shared `_all_cell_values` (player+pool zero drift), `_build_swing_zone_pools` 50 PA gate pooled across levels, pitch-group-specific, FAIL-SAFE. Ranked per exact (group×count/region×metric) slice. Gate doc'd in percentile-golden-gates.md (bsb-resources 6d11e2b5).
- Canonical ZSw/OSw/ZCon shown in-cell (ALL block, colored by ours). Freq% fixed = pitch-type usage by count (FB/Sec), dropped FB% row.
- Zac decisions pending: move both pages to END of report; design "Page C" combining A+B; Whiff%->ZCon%/OCtct% swap + canonical strip; positional-vs-evaluative color treatment. WORK-LAPTOP: real league pools + runtime UNRUN.
