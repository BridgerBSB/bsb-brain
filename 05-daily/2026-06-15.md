---
tags:
  - daily
created: '2026-06-15'
---
# 2026-06-15

## For future Claude
Centralized the pitcher unclassified-outing fallback into one shared helper and wired [[Arm Farm]]'s pitcher_analysis so position-player pitchers (e.g. [[Reylin Perez]]) render instead of blanking — and corrected a wrong assumption: position-player pitching is *already* in the KPI/tracker pools, no re-pin needed.

## Top 3 today
- Centralize the unclassified-outing fallback (kill the dual-query-path copy-paste) + wire pitcher_analysis acquisition mode
- Fix [[Arm Farm]] tracker Trends-tab YoY filter so partial-season 2026 stops vanishing
- Pull the Texas League runs-scored leaderboard for [[Sam]]

## Shipped
- **`bullpen_data.coerce_unclassified_outing`** — single source of truth for the fully-untagged-outing → one "Unclassified" (`UN` / `#888888`) bucket. 3 call sites: postgame CLI, postgame app, pitcher_analysis. Shipped `22c91f9c` on `feature/bullpen-reports`. [[Reylin Perez]] (gc 176305) now renders via `--pitcher-ids 176305` instead of a blank PDF.
- **Arm Farm tracker Trends-tab YoY row filter → level-only** (`min_pitches=1`), was gating on `_PCTILE_THRESHOLD` (300) which hid every partial-2026 row. Display-layer only, no re-pin. (The other 4 trackers were already level-only — Arm Farm was the lone diverger.)
- **Texas League runs-scored weekly SQL** for [[Sam]] — `sql-queries/texas-league-runs-scored-weekly.sql`, filter on `MLBAM.Schedule.LEAGUE_ID = 109` (the char `LEAGUE` column is NOT 'TL'). Player leaderboard + team/org rank with games column. → see [[texas-league-runs-scored]]
- **Docs:** `arm-farm.md` (new Unclassified-Outing Fallback section + position-player-pools correction + YoY-filter rule + acquisition cross-ref) and `cascade-orchestrators.md` (pit-pg auto-includes position pitchers). Synced + pushed all 4 worktrees (`d82400e0` / `7e6570c5` / `56dfbf23` / `41d4ca8f`).

## Wins
- Caught and reversed my own wrong claim before it shipped into a rule: I'd said position-player pitching "contaminates the pool / forces a re-pin." Read the actual code — every pool query is pitch-driven (`GROUP BY pv.pitcher_id`, no `player_type` gate) — so it's *already* counted everywhere, nothing to add, no re-pin. [[Sam]]/Zac flagged that they'd literally seen those stats recorded, which is what prompted the check.

## Friction
- Speculated about the KPI-weekly roster gate instead of reading it first (violates the "never speculate about data" rule). Cost a round-trip. Lesson reinforced: grep the gate, don't assert it.
- `/sync-rules` does a blanket `cp *.md` — propagated `bsb-resources`'s pre-existing uncommitted rule WIP onto the siblings as a side effect. Non-destructive (all in git), and it's the intended sync behavior (siblings were stale), so we left it. Worth remembering it's all-or-nothing, not file-scoped.

## Notes & ideas
- Open thread (work laptop): `git pull` in `bsb-wt-bullpen` + redeploy Arm Farm, then run pitcher_analysis for Reylin (`--pitcher-ids 176305`).
- Pools-decision left intentionally untouched: position-player mop-up innings *are* in the org pitcher percentile pool by design (mild noise). Don't add an exclusion without explicit direction.

## End-of-day reflection
Good "measure twice" day — the highest-value move was re-reading the code instead of trusting my own earlier framing. One shared helper now replaces three copy-pasted blocks.

## Links
- [[Arm Farm]]
- [[Reylin Perez]]
- [[Sam]]
- [[texas-league-runs-scored]]

---

# PM Session — BSB Brain build-out 🧠

## For future Claude
Turned the Obsidian vault from a flat snapshot into a real interconnected second-brain (~90+ linked nodes), fusing the personal baseball portfolio with the Astros canon, and stood up the global command + MCP + `/ingest` plumbing so any agent can read/grow it.

## Shipped (PM)
- **New rule `external-resource-capture.md`** — the fxtwitter X-fetch trick (`x.com` → `api.fxtwitter.com`, beats the 402 paywall) + the resource-capture-to-vault convention. Synced all 4 worktrees, committed + pushed (`12cbd0f4` / `81541350` / `26a9c25b` / `51e838c0`).
- **6 vault commands made global** (`~/.claude/commands/`) — work in every project now; `/context` renamed `/brief` (built-in collision). Confirmed **obsidian + recall MCP are user-global** (every agent reaches the brain).
- **New global `/ingest` command** — the RAG ingestion loop (feed any URL/repo/doc → fetch → atomic interlinked notes → wire into the graph).
- **Built the interconnected brain:** [[MOC-baseball-analytics]] + [[MOC-astros-engineering]] + `sql/MOC-groundcontrol-schema` + ~60 concept/people/project nodes. Personal modeling ↔ Astros canon now one graph.
- **Indexed Astros canon:** 15 metric concept notes (woba/wrc+/gcera/gcperf/oaa/paa-eo/netk…), the 74 rules themed into a MOC, the SQL schema map, [[gcera]]/[[gcperf]] methodology from the AR PDFs, the 192-page PD apprentice handbook map.
- **Deep-documented `personal-bsbres`** (full extraction, 1.5–3.3k-word notes): the Driveline **Independent Value family** (IPV/ISV/ILV/IOV + Execution RV — exact features, tree architecture, reliability curves), **Stuff+ DL / tjStuff+ / Mix+/Sync / Driveline hitting models**, **Big 3** + hitting biomechanics + 8 swing flaws + training implements + skill-dev + org training process, plus **my own pre-Astros force-plate velo/bat-speed modeling code** (RF/SVR/DT + SHAP + 0.65 R² gate) and the **youth HP trios/gainers** analytics + the **2024 4th-place Driveline AI comp** pipeline.
- **Inventoried all of bsb-resources** → [[bsb-resources-inventory]]; the 6 personal folders deep-analyzed into [[private-archive/README]].

## Wins (PM)
- **Beat the X paywall** with fxtwitter (raw x.com = 402, jina = 451, fxtwitter = 200) → pulled 9 loop-engineering threads into [[loop-engineering]].
- **Found the real context-rot lever:** Claude Code natively auto-loads ALL of `.claude/rules/` = **309.8k tokens (31%) every session** — not an MCP problem. Lever = `.claudecodeignore`. Excluded the graduation logs (~35k) as a test.

## Friction (PM)
- Went in circles on "why isn't MCP saving tokens" — MCP being *connected* doesn't stop the rules auto-load; **two separate switches**, only one flipped. Cost trust before it clicked.
- Was too incremental/"basic" early — user pushed hard for a real interconnected brain; fixed by building MOCs + atomic nodes + dense bidirectional links.
- **IG reel un-fetchable** (video, no text API; ddinstagram refused) — genuinely different from the X paywall fxtwitter solves.

## Notes & ideas (PM)
- **VERIFY next session:** run built-in `/context` in `bsb-resources` → did Memory files drop ~35k (309.8k → ~275k)? Confirms whether `.claudecodeignore` controls the rule auto-load (→ then we can slim to `blocking-rules.md` + MCP retrieval).
- **Real Astros project the graph surfaced:** [[send-from-2b]] send/hold model → extend to 1→3 / 2→H, ties straight into [[2h-1to3-run-attribution-status]].
- **Sharp finding:** every intrinsic-value model (IPV/ISV/Stuff+) can't model sequencing/arsenal — exactly the gap Mix+/Sequence+ fill.
- Next `/ingest` target: the `external-docs/` vendored model *code* (4S / Stuff-Model / tjStuff+) or another GitHub account.

## Open decision (unresolved)
- **Migrate the 6 personal folders to the Baseball-Ops org repo, or not?** Earlier: flagged former-company, DO-NOT-MIGRATE. Later today: "no guardrails, migrate them too." The brain (private) grows regardless; the **org-repo push is the one irreversible call** — needs a clear-eyed decision before anything goes up.

## Links (PM)
- [[MOC-baseball-analytics]] · [[MOC-astros-engineering]] · [[loop-engineering]] · [[bsb-resources-inventory]] · [[send-from-2b]] · [[2h-1to3-run-attribution-status]]
