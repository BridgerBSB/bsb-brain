---
created: {{date}}
tags: [daily]
---
# {{date}}

## For future Claude
<!-- one line: what happened today and why it matters -->

## Top 3 today

## Shipped

## Wins

## Friction

## Notes & ideas

## End-of-day reflection

## Links
- [[ ]]

## Session - Non-roster pitcher (Jona Widmann, gcid 252980)

Zac: sessions tagged `INT R` pop up in Arm Farm runs, and a signed guy shows
bullpens but is listed nowhere under HOU.

- `INT R` = `int` + `sched_type='R'` + `is_int_level=1` = DSL DR-Private
  intrasquads, i.e. **Live AB**. Already handled: postgame owns them, compared
  vs the DSL pool, never entering it. Zac's instinct (postgame not side) was right.
- **Widmann has no eBIS record at all** - `ebis_id` and `mlbam_id` both NULL, so
  there is no key to join PP_MASTER on. Roster surfaces INNER JOIN PP_MASTER;
  `Pitches_View` gates on nothing. Present in the data, invisible to every human
  surface. The real fix is eBIS-side, not code.
- The one-off crashed: `.get('level_code','')` returns **None**, not `''`, because
  a dict default fires only on a MISSING key. Fixed, plus a second latent site.
- **My first fix over-reached** and Zac's "will this mess anything else up?" is
  what caught it: `level_code` fed three renderers, not one. Narrowed to a
  separate `pool_level_code`. Lesson: count the consumers before overriding a
  shared variable.
- Narrowing surfaced a real App/Report parity break - the app printed
  "Percentiles based on 2026 INT" for Live AB while pulling the DSL pool. Fixed.
- Documented as `.claude/rules/non-roster-player-reports.md`, synced to all 4
  worktrees. Per-app table verified from source: **only Arm Farm** has the
  non-roster fallback; the other three silently skip, which is worse.

Remember: Zac verified a pre-change report still renders fine. The POST-fix run
is still unrun against a DB - do not claim it works end to end.

Loose end: `document-pattern` skill Step 4 copies the intangibles rule outside
that git repo (needs the `astros-intangibles/` level). Fixed by hand, not in the skill.

## Session 2 - Decision Outcomes ledger + dashboard (bsb-wt-modeling)

Zac's ask: a dashboard of everybody we promoted and everybody we released, where
the released guys went, how they performed, against the grade we had that day.

Went spec + mockups -> 7 modules -> three review rounds. Shipped `429171ed` ->
`b04da28a` on `feature/promotion-models`, plus a LINEAGE.md entry.

**Verified live:** 2025 boards on disk with no pin touched; `get_decisions(2026)`
= 61 promote / 53 release; Almonte lands where the design predicted.

**I was wrong twice and Zac caught both.**
- Told him historical backfill was impossible. It is `--year`, a flag that
  already existed. I argued from the pin layer instead of reading the scorer.
- Told him the release grade is current-level-only. It is MULTI-LEVEL POOLED.
  I read the promote scorer and assumed release matched. His instinct ("a guy
  kills AA then sucks at AAA, that context has to count") was right.

**The pattern that matters:** every bug found tonight failed in the same
direction - making the release model look MORE accurate than it is. Retirees
scored CONFIRMED because a retiree never resurfaces. A dropped DB connection
scored every release CONFIRMED. Never-resurfaced players got relabelled
TOO EARLY, gutting the headline tile. On a page whose only job is grading our
own decisions, that is the bias to keep hunting.

**Orchestration lesson:** 5 builders in parallel with no shared contract each
invented their own column spellings, and no single-module review caught it
because each module was internally consistent. The fix was one schema file
imported everywhere. Two of three agent rounds produced defects from parallel
agents not sharing state. The remaining findings I will fix by hand.

**Process:** 3 quoting failures from `python -c` one-liners in PowerShell (it
strips commas and single quotes). Commit a script. And local Python is 3.12
while Connect pins 3.11, so `py_compile` passing here proved nothing about a
backslash inside an f-string expression - the reviewer caught that and I
wrongly dismissed it first.

Watch: tempdb full on GCSQL02 at 23:48. My query triggered it; nightly pin jobs
may be affected.
