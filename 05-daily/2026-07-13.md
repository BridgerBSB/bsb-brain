# 2026-07-13


## Session (evening) — work-laptop MemoryError cluster + Slack add
Chased 3 `MemoryError` crashes on the work laptop (Python 3.14), all the same root cause: long batch jobs at mid-season data volume hitting the process memory ceiling, surfacing wherever the next allocation landed.
- **Advance batch (hitting + pitching):** fixed O(N^2) in-memory ZIP rebuild (`_build_series_zip_chunks` rebuilt the whole accumulated ZIP per PDF); then fixed cross-run RAM accumulation (`level_zips` now holds on-disk paths not bytes) + `gc.collect()` per series/level. Both scripts, both branches.
- **Zero-count weekly:** `gc.collect()` every 25 pages (org-wide 300+ figure PDF died allocating a 256KB zlib object). Figures were already closed — matplotlib reference cycles aren't reclaimed until gc runs.
- **Key insight for Zac:** "so many bugs lately" = ONE problem (memory ceiling), not many. Drivers: Python 3.14 upgrade on the work laptop (notes said 3.13) + mid-July data growth (66k YTD pitches, 110+ pitchers). Reliable workaround = run big jobs in smaller slices (fresh process each). Gave diagnostics to confirm 64-bit + RAM/pagefile.
- **Slack:** added `zzz_rodriguez_johnandy_1365743` / `C0BGF0P5N1H` (coach) to all 5 CSVs, pushed 4 worktrees.
- Zero-count weekly re-running on work laptop after pull; awaiting result.
