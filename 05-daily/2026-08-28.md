
## Session - magnet board becomes the third area of the hiring app

Built `/admin/magnets` end to end: one `magnet_doc` table, many documents.
`roster` is shared org data, `board:<email>` is one person's placements, and
the split is enforced by the server (POST /board writes the caller's board and
has no parameter for another one). Grid is DSL-AAA with DEV and IL strips, a
roster fraction that goes red over the limit, and a drawn diamond depth chart.

Zac corrected two things I had wrong: the board is DSL through AAA, not the big
leagues, and there is no DH in the minors. Deleted the MLB 26/28-in-September
rule along with the column rather than leaving it unreachable.

He asked for a grid restyle and retracted it in the same message - "DONT CHANGE
ANYTHING SORRY FALSE ALARM besides the 40 man tags." The field view is the part
he likes. Do not restyle the grid.

Ate the whole context window building this. The CSV import shipped UNVERIFIED -
the run that would have loaded his real 251-player file was interrupted.

## Session - magnet board, the 11-item spec (session 133c)

Shipped all 11 items Zac specced this morning, plus the earlier import
verification and the MLB restore. `hiring/main` f96122c -> 9ad3b74, plus two
query commits on `bsb-resources/feature/pd-goals`. 503 tests, up from 449.

**Decisions Zac took in-flight** (recorded in
`cage-sandbox/docs/magnet-board-spec-2026-08-28.md`):
- Current board seeded from eBIS, one shared doc; Project boards copy it.
- A rehabbing player is ACTIVE - dragged to the level he is actually at.
- MiLB 165 = FCL..AAA, active + 7-day IL only.
- DFA deferred until it is a real user problem.
- The week is Tue-Sun, not Mon-Sat (he corrected me before it shipped).
- DSL gets NO cap at all, and sits below the grid.

**The finding worth keeping:** the MiLB 165 is **161, not 165**. The pre-split
export could not distinguish a full-season IL from a 7-day one, so four FSIL
players were being counted against the domestic limit. The org has four more
spots than the board was showing. That was the entire point of splitting the IL
row and it paid off on the first real file.

**What I got wrong, three times, all the same shape:** I asserted about output I
had not looked at. User Management shipped printing raw HTML as literal text
with an empty level dropdown - so "Set level" could not change anyone's level -
because the template engine escapes every `{{ }}` on purpose and I handed it
markup. My tests checked the *function* that built the HTML and that a class
name appeared in the body; both passed on an unusable page. I also built chips
per table row when Zac had asked for squares at the bottom, and never noticed a
coordinator could not be *created* at all. All three were invisible to tests and
obvious the moment I rendered the page - which is a blocking rule I have and
skipped. Separately I told Zac a deploy had landed when I had only confirmed
that earlier commits were live.

**Next:** the COMPARE view - how differences between your project board and
somebody else's shared one show, on the grid and the depth chart. Zac's own
words: "idk how we show it". Starts as a design conversation.
