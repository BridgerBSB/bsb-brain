# 2026-08-15

## Session - EOY: ForceDeck sources + goals-page batch (11:59)

Waiting on Tina for the Player Care page, so: (1) worked out where the card's six
force-plate fill-ins actually live, (2) built a batch that renders every player's
Development Goals page on both decks.

- Commits `29e473b2`, `41a799cd`, `fe652391` on `feature/pd-goals`.
- Batch ran on the work laptop: **114 pages, 0 dropped goals, 0 failures.**
- **Phase census max = 2** on both sides. Zac was right to push back on the
  3-phase overflow - it is latent, not live. Second goals page PARKED.
- **I shipped a dead test.** Reported 160 pitcher goals with no current value;
  it was `g.get("actual")` against a key called `current`. 54 of 54 at exactly
  100%, and `no_current == n_gradeable` on every row - rule #19's own shape, in
  my analysis code. Fixed to raise on a missing key.
- Real finding: **Brutcher** Phase 2 all "No data" while Phase 1 resolves the
  same metrics. Raises whether the tally should count unevaluable goals.
- ForceDeck: use the EAV chain; the flat table has 1 of 6. Time to Takeoff is
  almost certainly `Contraction Time` - confirm with Alvaro, it is player-facing.

**Zac:** context is being eaten by rules loading - stop bulk-loading, pull per
decision. Next session = goals loose ends first, then 13 query outputs.
