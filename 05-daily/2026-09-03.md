---
created: 2026-09-03
tags: [daily]
---
# 2026-09-03

## For future Claude
Multipurpose app (hiring/main): closed the four deferred review findings, then
spent most of the day on the pitching tool with Zac and Sam using it live. The
lesson of the day is not a feature - it is that FOUR tests in this repo were
guarding an incidental string instead of the property they were named for, and
one of the four was mine, written today.

## Top 3 today
- Close the 2026-09-02 review findings (all four, plus the PDF)
- Depth-chart controls: arrows, drag-to-rank, UTIL and DEV onto the field
- Sam's ask: every placed object carries a name

## Shipped
Ten commits on `hiring/main`, ending `fb44d2d`.
- `f940100` the four findings: `/api/scenarios` was readable by any candidate
  while whoami's own comment said it must not be; invite actions and session
  detail resolved ids with no track check; `/sample/open` wrote two rows a
  press with no limit; the evaluator's plan drew a bare rectangle with no mound.
- `a864950` **the Sandbox had never applied `unlimited_items` on either track.**
  Zac's hitting uncapping "worked" only through a frozen invite. The Sandbox
  posts an inline scenario and the id is forced to `custom`, which deliberately
  names no file.
- `09f88f9` + `8dd53b5` the depth chart: arrows were being swallowed by a
  dragstart (they sit inside a draggable magnet), derived men had no arrows at
  all, and every drag appended to the bottom because `p.slot` was set before
  `slotList` ran. The drop point decides the rank now.
- `de1ccd8` + `4be2aa4` UTIL under 1B, DEV under 3B.
- `e7769e3` 45 palette items were drawing with no name on them.

## Wins
- The node-extraction harness. The board and the cage app are single-file
  IIFEs, so nothing is importable - pulling the real functions out by brace
  matching and running them with two stubs gave real tests with no server,
  no session, no login. `tools/test_depth_order.js` (40) and
  `tools/render_placements.js` (138 item x rotation) both came out of that.

## Friction
- **The playwright harness died and I could not revive it.** Admin sign-in
  401s after repeated driver runs; survived a server restart and two re-seeds.
  Three attempts, then stopped. Cost: nothing from today has been seen in a
  real browser.
- **I asserted instead of testing, and Zac caught it.** I told him "dragging
  was never affected" - true of whether the placement took, false about where
  the man landed, which is what he was asking. He said he would record it again
  "to see if you're bullshitting or not". He was right. Testing it found three
  bugs, two in the fix I had shipped an hour earlier.
- Zac: "wow i thought this wouldve been a w ay simpler change...?" - fair. The
  one-liner he asked for WAS one line; it grew because the one-liner did not
  work, for a reason that predated it.

## Notes & ideas
- Zac's call, asked and answered: PEOPLE are uncapped too, not just equipment.
  I flagged that it removes the scarcity the tradeoff question is about; he
  decided anyway and the briefs were reworded to match.
- Still his to eyeball on the deploy: UTIL/DEV positions, drag-to-rank, and
  whether the arrows still swallow clicks.

## End-of-day reflection
Four tests guarding defects, in one day, in one repo:
`test_the_evaluator_page_says_mound_not_cage` and the e2e walkthrough both
asserted the literal `"Mound 1"` - which was the defect. The depth-arrows test
asserted `p.slot`, incidental to the ORDER its name claims. And my own new
naming guard asked whether a placement emitted any `<text>`, which can never
fail because `iconMark` draws two-letter initials as a text node - two
injections came back green against it.

The tell is the same every time: the test's NAME states a property, its
assertion names a string. Injecting the bug on purpose is the only thing that
found any of them.

## Session a3f1 - Astro World: Aerollo labels shipped, Travel Hub ported (PR #65)
- Aerollo #62-#64 merged + live: label chips fit descenders + show names; 12x5 shade grid + exact hex; tiled custom board backgrounds; labels shared across every board with search; background reaches the footer.
- Travel Hub ported from the colleague's HTML into CoordinatorHUB (#65 OPEN): staff = Coordinator/Admin levels from Manage access; Coverage + Admin admin-only; coordinators own their trips; 5 new tables via Admin > Database. Source "today" was UTC - fixed to Central.
- Zac corrected me: Azure may well reach GCSQL02 (Microsoft, internal) - nobody has tried. Feed = gc2 | statsapi env-selected + Test connection. MLB Stats API verified live for all 8 affiliates.
- Zac's review items on #65 to attack next (verbatim in recall 568d99b9b174e1c9): current-year season w/ history kept, affiliate logos, games from the DB in Master Schedule, trip form minus functional area + times, affiliate blank by default, no Seasons admin pane.
- Also noted (hush): magnet board may become an Astro World program later. Access: coordinators ALREADY read the whole site; pending call is SITE_LIVE / PRELAUNCH_MIN_ROLE.
