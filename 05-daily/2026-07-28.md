# 2026-07-28


## Session — Barrelsville hitter_analysis Page A/B (feature/barrelsville)

Iterated the per-player hitter PDF (`barrelsville/scripts/hitter_analysis.py`). All pushed, HEAD `34840730`, synthetic-render-verified, UNRUN vs DB.

- **Page A**: dropped Whiff% → added **ZCon%/OCtct%** (geometric zone-split contact) with canonical CSC-weighted parens in the ALL block only (like Page B). Added **Dmg%** row. (`bd0f8f73`, `3e6256ae`)
- **Page C** prototyped twice (region×count bat speed `50da3d79`; "Production by Count" `3628721a`) then **pulled** — Zac spotted it was structurally identical to Page A once region was dropped. Recoverable from those hashes. (`17d712d1`)
- **Pages A/B moved to positions 4/5** (after EV/Whiff), after I first wrongly put them at the end (8/9). (`34840730`)

**Zac to remember:** "this PDF isn't really ever finished — I like to edit it for analysis." Treat hitter_analysis as a living analysis doc, no done-state. Design lesson: a "combination" page must keep a NEW axis (region×count) or it collapses onto an existing page — dropping region to fit 3 groups + metrics made Page C = Page A.

## Session - R&D heavy-compute inventory (bsb-resources/feature/pd-goals)

Swept ALL 4 worktrees with 4 parallel agents (~978K subagent tokens, none of it in main context) and shipped `pd-goals/docs/plans/2026-07-28-rd-heavy-compute-inventory.md`. Sent to R&D; awaiting reply. HEAD `3edbb62b`.

- **Ranked ask:** (1) `batter_ev_p95` - a 3-APP problem, not just Arm Farm; Barrelsville has 10 heavy-CTE call sites left and **PD Engine never got the fix at all** (`org_kpi_data.py:640`, `eoy_hitting_percentiles.py:47`). (2) Fielding pooled combos - 3,600 DataFrames/domain-year, ~132 min daily, but pure-pandas so it is CPU on a viz server; the real DB cost is the 8 base slices (`monthly_fielders` MEASURED 8,203 s). (3) League percentile pools - largest unpinned aggregation, 268 `PERCENTILE_CONT` in intangibles alone.
- **Insight that changed the doc:** compliance + trackers + rolling chart + drift report are all "aggregate one player over an arbitrary date range" -> **ONE daily per-player per-metric fact table** serves all four. Now leads the suggested sequence.
- **Cadence corrected org-wide:** ALL Connect pin jobs run **every 12 h (twice daily)**, not 6h. ~12 sites across 4 worktrees (`ff4922e7`, `0438efa1`, `eb6efd07`, `db2b5ccb`). LINEAGE entry written; June offload preface Section 2 banner-stamped as superseded.

**Zac to remember (editorial rule for anything going outside the group):** do NOT disclose our own inconsistencies to R&D - "this is a me thing... they're just the ones who will help us get shit into a db." Cut the 25-vs-50-PA gate mismatch, "three other modules rebuild separately", the DuckDB dead-code confession, the "Open items on our side" section, and "one definition, four implementations". Also cut the whole "Who owns what" ownership/parity section (reverted `3edbb62b`). Frame duplication as shared *need*, never as our error.

**Second thing to remember:** storage pins (goals / PRP / transition / EOY notes) belong in the DB eventually too - not for speed, for **joinability**. "the more stuff in the db the more reciprocation across all domains."
