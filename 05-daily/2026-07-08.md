
## Session block — Promote/Release PDF + promo-engine app polish (wrap 01:30)
- Built the combined **PROMOTE & RELEASE MODELS** doc to 14 pages (real shap beeswarms via shap pkg, real sklearn ROC + calibration). Page-by-page review with Zac; strict rules enforced: no "we", no em dashes, "highest weighted stat" not "headline", projected/predicted never definitive, no "not player-facing"/"never auto-cut" disclaimers.
- promo-engine app: drawer 50vw + 3-leg bars + complex weights; removed player-picker; MLB-credit release (DS Option C: decompose target, deterministic serve offset, no leak); "Release + MLB" + "72/28" test lenses; merged two how-to-read sections into one "How to Read the Models" with the combined PDF bundled+manifested; markers moved left of name; documented the thin-sample marker (Zac's "red asterisk").
- DS ruling: don't feed career_mlb as a feature (25% of target = leak, R² 0.43->0.68 fake). Split it out, add back at serve. Blend re-learns to 72/28 but HELD at 61/39 pending Zac.
- Zac emphatic on: don't over-explain in app (cringe), no disclaimers, "why the fuck isn't this like I asked" re combined-PDF-in-app (I'd missed wiring it). Wants to test lenses live before picking headline.
- OPEN for tomorrow: MASHER badge remove-vs-explain, drawer side-by-side layout, headline lens choice, adopt 72/28?, confirm red-asterisk=thin-sample marker.

## Session — PD Engine PRP Report incident (recovered + guarded)
- **Display bug:** position players showed stale/old PRP content. Root: deployed app couldn't reach the pin (`CONNECT_API_KEY` missing in PD Engine Vars after redeploy) → `load_prp_records` fell back to the `prp_store.csv` seed (baseline content, 0/8 status). Fix: set the Var in Vars tab + **restart the content** (Vars only re-read on restart). Pitchers looked fine only because their seed baseline was current; position players' recent app-completions weren't in the seed.
- **Mass wipe:** `prp_records_2026` pin dropped 48→10 players-with-progress. The 48-player high-water version (51802) had been **pruned by Connect** (version count 24→11) → unrecoverable. BUT all writeup CONTENT survived (verified Neyens "SwDec and Barrels"/"Increase zSwing to 52%", Moss). Only the green "done" checkmarks reset. Restored today's work from version **52015** via `recover_prp_pin.py --restore`.
- **Root cause of the wipe:** `pin_prp_records.py` read the pin via `load_prp_records()`, which falls back to the blank 0/8 seed on a read miss → every player took the "kept" path carrying blank status → wrote that wipe back. (Or a save on a thin read.)
- **Shipped (feature/pd-goals, pushed):** `_pin_write` anti-shrink guard (refuse >3 player-progress drop unless force) + `pin_prp_records.py` strict read (abort if unreadable) + new rule `prp-pin-wipe-recovery.md` runbook.
- **My mistakes to remember:** (1) I read LOCAL CSVs from the personal laptop and reported them as the live pin state — connect2.astros.com does NOT resolve here, so every loader silently falls back to bundled CSV. Ground truth for pins is ONLY the work laptop. (2) Raised "data pruned/lost" as worst-case before confirming content survived — should check content vs status first.
- **Zac direction:** "document this and ensure you know to do this" — recovery runbook written. There are PRP features to adjust next session (not yet specified).

## Session — Goal Compliance year/phase + goal-parser audit spec
- **Shipped (feature/pd-goals, pushed `06827186`→`cb7dda87`):** Compliance revamped to **Year + Phase** selectable + durable (2026 Phase 1 comparable in 2029). `_stamp_phases` tags each cycle season+phase_label ("Phase N" = player's Nth cycle that season, resets yearly, sparse by construction). Universe is **goals-driven, not roster** — released players stay in past phases. All-phase detail; app aggregates per (Year, Phase) via `aggregate_for_phase`; **"All phases (year overall)" is the DEFAULT** (pools every phase — Zac's call). Live-recompute button + rebuilt pin fix the two bugs (blank Start/End dates, new goals not showing).
- **Decisions (with Zac):** phases = per-player auto-increment; year pickers data-driven (2027 appears on its own); direction should **anchor on each metric's HiB/LiB** (via /audit-metric) + word + symbol, not one alone; source/intern fixes go in BOTH goals pin + prp_records pin.
- **Goal-parser accuracy audit SPEC** written from Zac's Phase-2 QA sweep (2 compliance screenshots + table.tsv + inline list) — verbatim case inventory + 6 workstreams (direction "green-when-underperforming", metric-not-populating avg-velo/InZ/OSw%/GB%/PL1B, GC2 parity bugs, parenthetical→false-subjective, PAA/EO mis-subjective, new parser support, S&C subjective-until-wired, connected-cycle same-day overwrite). Flow: `/clear` → `/orchestrate` against the spec.
- **Deferred:** page's 4 `st.tabs` → keyed `st.radio` (Year/Phase change snaps to first tab) — needs a live run. Open spec items: S&C label wording; `SL+ in slack (gillis)` meaning.
- **NEXT:** work-laptop `git pull` → `pin_compliance.py --season 2026` → redeploy PD Engine (`--app-id 79f52369…`), then clear + orchestrate the parser audit.

## Session — Org KPI OF sort + Marcus Brown + slack-channels-csv skill
- **Org KPI report:** MLB Outfield page now ranks by OAA (primary), PAA/EO tiebreak; MiLB unchanged. `_draw_domain_page` guard on `level_codes==['mlb']`. `c2d0f846` + config comment `7129017c`. CLI-only (Monday cascade), no deploy. (Long detour: user's "still wrong" was a stale PDF — the fix was correct; rank-annotation-independent-of-row-order is the tell.)
- **Marcus Brown Slack channel:** was a dead standalone `z_` row (athlete id stranded in channel_id col, which delivery ignores). Fix = ONE row per player: athlete id in the `z_channel_id` COLUMN of the `zzz_` row. Consolidated + deleted standalone, all 5 CSVs, pushed 4 worktrees (`642757e0`+).
- **Decision / remember:** the RIGHT way for athlete channels = `z_channel_id` column, NOT a separate `z_` row — `deliver.py::load_channel_mapping` routes by gc_id reading the column, never by channel_name. Documented BLOCKING in `slack-channels-sync.md` (corrected the old misleading "add a new z_ row" line), synced 4 worktrees.
- **Audit found real breakage:** 9 players with broken athlete delivery (8 fixable), 12 zzz rows with empty channel_id (coach→overflow), 3 junk rows, 2 orphan z_ rows, 18 redundant dups, 10 name-suffix≠gc_id (leave). SWEEP pending Zac's go-ahead.
- **Skill:** `add-slack-channels` → **`slack-channels-csv`** (add/fix/audit lifecycle). `ca4c3ec0`.

## Session — promo-engine absolute grades + app refinements + code review (wrap 19:01)
- **Branch `feature/promotion-models` (bsb-wt-modeling), all committed+pushed `17125732`→`11a82330`.** Two batches via subagent-driven implement→verify loop.
- **Absolute grades:** both models default to absolute per-player 0-100 (Promote=calibrated odds, Release=61/39 blend shown as value), shared Grade-basis toggle Player/Org/Level. Release headline STAYS 61/39 (projection-only REJECTED — age-for-level is predictive, Zac). MLB-credit lens TABLED. New pin cols GRADE_ABS/ORG + RISK_ABS/ORG/LVL.
- **App refinements:** PA/IP (Lvl)+(Rel) side-split (rel_sample = pooled cross-level sample, new release pin col); markers reworded (Promote-sample, icons kept); color all add-cols except Quadrant (within-column percentile); removed 70/30; keyed Sort-by; append-added-columns-at-right (core cols locked, no reshuffle). F (full table state) = AgGrid, deferred.
- **Parquet crash fix `7e8ec2f4`:** new-level rows left low_sample/insufficient object → ArrowInvalid; scorers coerce bool before pin_write; `--output *.parquet`.
- **Deep research for Sam (boss):** AUC tables (promote H 0.82/P 0.76, release no-future 0.85/0.81, blend 0.777/0.764, all reproduced); FCL→A jump IS transition-validated but thin (~200 FCL positives); promote odds calibrated (isotonic, per-band), release NOT calibrated (RISK=score not probability); censoring = 5,390 young players held out of release training (survival right-censoring, incl. Holliday/Rada). K% vs K-BB% visual (K% +0.0283 AUC, #1 SHAP) sent to Zac.
- **CODE REVIEW (high, whole-diff): 10 defects, 4 ours.** Captured in `pd-goals/modeling/docs/plans/2026-07-08-post-batch-code-review-issues.md` (`11a82330`). A1 Org-wide rank within-band not system-wide; A2 drawer shows two Promote numbers (promote history logs GRADE not GRADE_ABS); A3 just-promoted RISK_ABS is rank not absolute; A4 sparkline metric-swap. Pre-existing: B1 promote hardcoded 2021-2024, B2 release 2025 hardcode, B3 shared-pin clobber, B4 TLS process-wide. **DO NOT deploy promo-engine until A1-A4 fixed.**
- **Zac feedback (remember):** NO em dashes ever (forwards to boss, reads as AI — saved `feedback_no_em_dashes.md`); state DATA not causal assumptions in boss-facing text ("K% +0.028 AUC" not "command not formed"); was overwhelmed by 10-issue count — reframe: caught pre-deploy, process worked.
- **Root-cause lesson to make durable:** added the absolute-grade convention task-by-task, missed downstream consumers (history/org-rank/fallback). Needs a "headline-metric-consumer parity" rule in `.claude/rules/` (NOT yet written — flagged). Standing process: run `/code-review` as a GATE before work-laptop deploy.
- **NEXT:** discuss deeper w/ fresh context, then `/plan` Phase 1 (fix A1-A4) before deploy; `/explain` any single issue first if wanted.

## Session — goal-parser + compliance audit (orchestrated)
- Orchestrated the goal-parser accuracy audit (discovery workflow -> implement). Shipped W1 parser direction grammar (kills the green-when-underperforming bug: trailing < AND + = FLOOR, 41%+==41%<), W6 PRP same-day-overwrite (no phantom Phase), and batch-2 parser fixes from Zac Phase-2 compliance CSV (1stPInZ/2K Proj target-glued-digit, bare handedness LHH/RHH/LHP/RHP, FPInZ/FF-IZ aliases, Fo Rel/CI subjective).
- Fixed live crash: _season->_year NameError on Compliance tab. Built fix_accidental_phase.py (Chirinos+Alvarez stray Phase 2).
- Decisions (Zac): S&C strength goals stay generic Subjective for now; SL+ 41%+ = slider InZ% floor; phase_label is derived so deleting a bad cycle re-ranks automatically; goals pin is authoritative (do NOT run pin_goals.py after editing the pin).
- Learned: app redeploy does NOT refresh pins; compliance table BAKES the parse at pin-build time (must re-pin after pulling new parser). NEW rule mid-session: no em/en dashes in output (boss-facing).
- Every parser change golden-diffed against 272 real goals.csv goals before commit.
