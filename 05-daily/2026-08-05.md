# 2026-08-05

## Session - z_ channels for 5 players + slack-channels-csv skill corrected

Added athlete channels for Loperfido (106480), Mendez (264917), Rivas (1299133),
Colina (1299134), Arias (1329926) - set on each player's existing zzz_ row, never
a standalone z_ row. Since Tuesday the weekly fielding/hitting reports are
athlete-only, so these five had been landing in coach channels every Monday.
No-athlete-channel count 80 -> 75.

Then fixed the skill that had just misled me: it still said 5 CSV copies after we
deleted the 5th on Aug 3, and demanded a 5-way md5 that could never pass.

**Two things bit me and are now written into the rules.** `grep -c $'\r'` does
not detect CRLF in Git Bash - it reported CRLF for all four copies while xxd
showed plain LF, so two string-replace passes matched the wrong line ending and
silently found nothing. And the four copies are not uniform: bullpen is CRLF,
the other three LF, so raw md5 differs while content is identical. Compare
normalized content, and preserve each line's own ending when editing.

Also pulled bullpen, which was 33 commits behind (a whole toolbox/ module someone
else pushed). That cleared the barrelsville commit stranded by Tuesday's SSH
aborts too - all four worktrees are current for the first time in days.

**Open:** 75 players still have no athlete channel (Slack-admin work, offered the
list twice) · next Monday's `Collapsed N -> M` log line is still the real proof
of the double-send fix · BB% percentile parked by Zac.

## Decision Outcomes - the dead-comparison sweep (session 97a3, bsb-wt-modeling)

One question from Zac - *"spence went from aaa to mlb, why is there an extra AA in here?"* - turned into four instances of the same defect.

**The class:** a display sourced from a value that by construction can only have one answer. `next_level >= level_to` compared a value against itself, because the resolver forces `next_level` to equal `level_to` before anything reads it. `next_level` never meant "where he is now"; it means "the stop we judged". Four copies on one page. Fixing the first three is what made the fourth visible, as the page started contradicting itself.

**The one that mattered** was not the one he could see. `_promote_bucket` fed the *Sent back down* tile, so a player optioned down who then got hurt or took 6 PA was counted as having **held** his promotion. Tile went 7 to 8 after the fix.

Also shipped: `level_from` moved from the trailing-90d modal level to the previous stint (the modal was grading a promotion out of AAA with the player's AA number - the wrong level was also the flattering one); a repair path, because `decision_id` does not contain `level_from` so a re-run could never reach frozen rows; a `_pin_exists` guard, because `pins` reports a timeout as "does not exist"; and a send-down chip so the board-derived case carries the same weight as a detected demote.

**My own bug:** `--dry-run --repair` was appending for real. That is the one command you hand someone so they can look before touching anything.

**Zac said, worth keeping:**
- *"i've never seen u do shit like this"* - fair. More errors than usual, and two were confident wrong calls: I said the board hadn't caught up with a callup when the player had been optioned **down**, and guessed the one stale ledger row was a phantom promotion when it is an ungraded demote. Same mistake both times - explaining what numbers **meant** before checking which **rows** they came from.
- *"oh why didnt u just say that lmaoooo"* - after an hour on two disagreeing percentiles, the answer was "one dot per move, one row per player". The page header said `52 players / 64 moves` the whole time.
- *"thought this would be way simpler"* - the fix he asked for was one line. The complexity was that the same broken comparison sat in four places.
- *"this has hogged a bunch of time we should be using elsewhere"* - parked here deliberately.

**Documented:** blocking rule #19 + new `tautological-display.md`, `prp-pin-wipe-recovery.md` §4b, LINEAGE entry. Rules propagated byte-identical to all 5 worktrees.

**Connect:** Min processes 0 -> 1 on the Promotion Model content. That was the intermittent "could not reach Connect" going back to 08-04, and it was never code.

## Session - Postgame V2: spec + card prototype (bsb-wt-bullpen, 4089)

Zac sent a Pitch Profiler card screenshot and asked to shift the Arm Farm
postgame pitcher report to that shape for 2027. Spec'd it, built the layout
prototype, rendered it, closed every open decision. PDF only in v1.

**The headline finding:** RV and gcPerf are the same number.
`gcPerf = 50 - 1500*AVG(rv)`, so `RV/100 = (50-gcPerf)/15`. The card was going
to ship both as if they were independent evidence. Zac: "use gcperf in the RV
case!!" - gcPerf everywhere now.

**Rendering the prototype paid off immediately** - four defects `py_compile` is
blind to, worst being percentile colors INVERTED (gcERA at the 97th painted
red, because the data layer orients low-is-better to goodness and the renderer
inverted it again). Also `Overall` printing `10.0` instead of `100.0` from a
leading-zero strip that ate the zero inside the number.

**Two things I asked Zac that I should have looked up myself.** Weight is
`MLBAM.Players.weight`, two rows below height columns we already join - sitting
in a committed schema snapshot. And "which Loc?" was a non-question; ours is the
only Loc. He called it: "u already have all teh fucking documentation for this."
Wrote memory `gc2-schema-snapshot-location` so the snapshot gets checked first.

**That dig found a real bug though:** `sched-types.md` had TWO wrong entries -
`V` is Live BP not "Veloz", `I` is Intersquad not "Instrumented", and 15 types
exist where the rule listed 5. That file auto-loads on every .py/.sql edit, so
both errors had been feeding every agent for months. Fixed + synced 4 worktrees.
Also answered his original question: LiveBP = sched_type 'V', already handled.

**Decisions Zac made:** percentile gate = 300 in the focus (pitch type, group,
hand, count, TTO) - one number, no per-tuple table. Palette stays house
(red bad / green good), not the mock's Savant red-is-good. Delivery deferred:
"delivery is the last of our worries ... we are just focused on report creation."

**Remember:** he wants a recommendation next session on how to one-shot the
build - orchestrate vs goal vs plain. My answer: not orchestrate for the
implementation (17 metrics, one file, shared references - writers would
collide), possibly a research-only fan-out, /goal scoped to end at code+tests
not "verified vs GC2". And ask him about the two-column game-vs-season
treatment before building - it is the only part I designed rather than derived.


## Session - swing shape: the coachability question got answered (bsb-resources, feature/pd-goals)

Zac ran `--shift` on the work laptop and came back unsure what it meant. It is the test
that decides whether the org gets a coachable loft lever, so it was worth getting right.

**+1.00 pts of top-zone whiff per degree of loft, within-hitter** (CI +0.43 to +1.58,
n=373). Not zero, so loft IS movable - which retires a claim the deck had been making in
three separate places. But small: hitters only move ~1.4 deg across a season, and the
same change costs hard contact. Still no degree targets.

**I got the size wrong first and had to correct it to him.** Said "half the league curve"
off a `+2.0` printed next to the output - which turned out to be a hardcoded literal,
orphaned when the survivorship code was deleted in July. Recomputed from the actual frame:
MLB/AAA +1.35, AA/A+/A +1.19, FCL/DSL +1.00. Inflation is 35%, not 2x. The gradient story
survives and is cleaner - the within-hitter number lands exactly on the least-selected tier.

**The thing worth remembering: measure reliability before ranking players on anything.**
Two metrics died to a ten-line split-half test with loft/tilt as the positive control. The
contact-depth "window" came back at **-0.08** against loft's +0.99 - it was measuring coin
flips, because a 5-point threshold sat at 0.4 of a bin's standard error. And UNTAPPED, the
deck's one actionable item, was firing on nearly every card because "band below his own
average" is true half the time by construction; put through a real test it survives on 3 of
145. The survivors are excellent - Pena makes contact up in the zone (13% whiff) but only
33% hard-hit there vs 55% everywhere else.

Ran a 47-agent code review. 10 confirmed findings, fixed 9.

**Zac said:** nothing on the deck contents yet - opened it and called /wrap. Also called me
out, fairly, for sending him to the work laptop to rebuild a PDF I can build here.
`--extract` is the only step that needs the DB.
