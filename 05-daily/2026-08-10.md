# 2026-08-10

## Session 64ec - EOY images to the MLB pool, + Camden's pitcher handoff

**bsb-resources / feature/pd-goals.** Three commits, all pushed.

- **Every percentile-coloured IMAGE in the EOY deck now ranks vs MLB** (`39c76dff`).
  Zac spotted the inconsistency while asking a different question: the P13 direction
  roses were MLB, but the catcher NetK quadrants in the SAME diamond, the P15/P17
  roses, and the P19 blocking panels were all hi-level. Nothing rendered wrong, which
  is why it survived - you cannot see a pool in a colour.
  **The rule that came out of it: a colour cannot carry a label, a column can.** So
  images go to MLB and tables keep the %Hi + %MLB pair.
  Second-order catch: the qualified-catcher gate had to move WITH the pool, or an
  MLB pool intersected with the hi-level qualified set collapses the distribution.
- **The subtitle overflow.** My first caption draft measured 5.78in against a 5.45in
  axes and clipped into the KPI table. Only the render caught it - measured four
  candidates and shipped the one with the same margin the original had. Rendered P13
  (position AND catcher), P15, P19 at production dpi.
- **Pitcher handoff for Camden** (`8ceabbcf`) + artifact. Writing it turned up a real
  blocker: `get_college_pool` and `get_unofficial_pro_pool` probe `pv.batter_id` only,
  so **a drafted pitcher can only ever be situation 1**. The affiliate pool already
  solves this and nobody carried it across.
- **Bullpens stay excluded** (`9087e1ca`). I raised it as an open question on the
  wrong premise - said the position-side exclusion ("a bullpen has no batter") would
  not apply to a pitcher. Zac: not worth showing. He is right, and the real principle
  is **competition only**, the same reason `P` Batting Practice was cut. Net win: the
  unofficial tuple is now identical both sides, `E`/`I`/`V`, nothing to diverge.

**Zac said to remember:** Camden owns the pitcher LAYOUT and is building his own - we
hand over the chassis, not page designs. Parked here on purpose; he comes back with
issues or if Camden needs more explanation.

**Unanswered, do not treat as declined:** the EOY Send handler does not call
`save_eoy_note`, so a rewrite-then-Send without Save ships new text while the pin keeps
the old. Offered save-on-send vs warn-on-send.

## AstroWorld - content authoring, access levels, HUBs (session 672c)

Twelve PRs merged (#28-#39), two open at wrap (#40, #41).

**Shipped:** PowerPoint as a real upload kind (a .pptx used to store as a video and
render a player that could never play). Page and section intro text, authored ON the
page - the old `blurb` field became its lead paragraph and its edit box was removed
from Manage sections. Four ranked access levels (Admin > Coordinator > Manager >
Viewer), editing admin-only, `editor` retired along with the `section-editor` Entra
back door. CoordinatorHUB + ManagerHUB, hidden from anyone who cannot open them.
`ACCESS.md` runbook at the repo root. "Make it Pop" plus bold/italic/bullets.

**Decisions:** Sam rejected the navy panel for Pop ("i don't love the blue back") and
picked a short orange rule with 20px navy text. Zac: intro text is FULL WIDTH - said
twice, because the first fix only covered page intros and left sections capped. The
width cap is now removed entirely rather than made optional.

**The bad one:** I told Zac to click Load structure to create the HUBs. Its upsert
overwrote `title` on all 95 canonical pages and reverted every rename he had made. I
then understated it as "5 pages"; he caught it ("1st and 3rd is different"). The old
titles are unrecoverable - nothing in the app or DB ever recorded a previous title.
Fixed at the cause in #41, and the protection is a constant plus a test proven to go
red, not a comment.

**Two of my own tests were tautological and hid real bugs.** The rich-text test
asserted `flat(s)` against `flat(s)` for any string containing an asterisk, so it
passed green while "2 * 3 * 4 = 24" was rendering as "2  3  4 = 24". Same class as the
`next_level >= level_to` sweep. Both fixed and each proven to fail without the fix.

**Standing gap:** no change log. What changed, when, by whom, and what it WAS. That is
the specific thing that made today's reverted titles unrecoverable, and Zac had already
raised it before it bit us.
