
## EOY deck: three fixes, one of them overdue (bsb-resources / feature/pd-goals)

**The render loop, finally.** Zac raised "why does it feel slower the more notes
I add" four separate times and I kept answering with a timing breakdown instead
of changing anything. He called it: *"this is what ive been asking you to do -
and you havent addressed it."* He was right. The notes text was part of the PDF
cache key, so every saved keystroke redrew all 22 pages plus rasterizing, ~40s;
his own log showed 5 rebuilds in 2.5 minutes from 3 boxes. The deck never got
heavier - he was paying the render more times, which is exactly why it tracked
with how much he was writing rather than with the player. `dece64e0`.

His diagnosis arrived before any measurement of mine. Worth remembering: when he
says a thing feels slower, take it as a data point, not a feeling to explain away.

**The fix keeps the dangerous half honest.** Preview builds from a snapshot; Send
does NOT, and rebuilds live. A delivered deck missing the coordinator's last note
would be far worse than a slow one, and I proved it in a lifecycle simulation
rather than asserting it.

**PC&P box.** Real S&C text ran off the page. Fixed by making the font yield to
the text (6.0pt floor, then warn rather than clip) and by rebalancing panel
heights instead of growing the stack, so the pitcher deck keeps its page bottom.
My first attempt fixed the overflow and drove the recs straight through the P2:P1
archetypes - caught only because I ran `layout_issues` on the figure instead of
reading my own diff.

**A rule of ours was wrong.** `streamlit-inline-images.md` said download buttons
do not use the media endpoint and are unaffected. They do, and they 404 the same
way once the entry is reaped. That file auto-loads, so the wrong claim had been
steering debugging away from the button. Corrected with his log as evidence.

**Still open:** the PoC page renders a third of its density panel off the page in
the app while the committed code reproduces correctly twice locally - the
evidence says stale Connect bundle, and the renderer stamp (`38e76c9a`) settles
it in five seconds. Also: `eoy_layout_check` drops any string repeating 4+ times
as a "data series", which is exactly what axis tick labels do - so the one
element class that reveals a mispositioned axes is the one the guard cannot see.

**Also:** built `pcp_backend_2026-08-27.zip` for Tina (SQL + data module +
methodology), README leading with the hamstring-is-average-not-max ingest gap.
