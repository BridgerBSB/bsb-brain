# 2026-07-17

## Opportunities Bot — 4-category rework SHIPPED + one-command runner (session began 2026-07-15)
- Built the 4-category position-player rework (Offense/Baserunning/Fielding/Catching, 5 worst per area) on `feature/pd-goals`, ran it LIVE on 23 players, all areas populated. Commit `70e1b6da` (rework) -> label/reps fixes after.
- Fielding: gate 10->30 (general), bot-local >30 pool (live PD Goals app UNTOUCHED), OF arm = ONE value all-pos/all-level; value pooled all-levels, percentile vs current level. Catching split to own category + CS%/Depth/Bounce%/AccPen + 4 quadrant NetK (catcher's view, split plate_x=0 & zone mid-z 2.4235ft = 0.4025*6.0212, ~300/quadrant, overall NetK kept). Baserunning = new native `generate_baserunning_opportunities.py`.
- `run_opportunities.ps1` one command runs all exports + ranks + prints box; default = full HOU MiLB roster (`list_org_roster.py`), `opportunities/gcids.txt` = documented 23-player opt-in subset. `render_box.py` = auto box table + all outputs collected to `opportunities/output/`.
- Zac fixes: dropped sprint/SB from Offense; labels Blocking->SurPP, Framing->FramRAA; dropped redundant Ctct% from Offense (keep Whf%); restored rep `(n)` counts.
- Decisions: NO exchange ceiling (bottom-25% surfacing is the point, don't re-offer). Shared percentiles.py fielding gate 10->30 across apps = FUTURE (verification-backlog). Quadrant top/bottom line is fixed-height, could go per-batter later.
- NEXT: chat Offense (Phase 3) rework — deferred; hitter export (barrelsville) untouched, trickier target-distance metrics + multi-level level-tagging/transition-delta. Then re-run to see corrected labels/reps live.

## Session — PAA/EO investigation + Fielder Progression fixes
- **PAA/EO vs GC2 RESOLVED:** our fielding PAA = per-position-then-sum (matches GC2 metrics table 6.506, Sacco); GC2 game-log tab pools globally (6.833). Kept per-position (correct). New rule fielding-paa-perpos-vs-global.md (all 4 worktrees). Ruled out grain/scope/TDM/lineup/rounding. NEXT SESSION: discuss switching to global pooling + blast radius.
- **Fielder Progression fixes (feature/astros-intangibles, head 157dc87f):** CS%/Bounce% double-x100, BlockRAA blank (name alias), MoM=current-vs-prior month, YTD=net (can go negative). Confirmed DSL Reg-only + in run_monday.ps1 (OF IF C -> 2 channels). Zac testing on work laptop.
- Decision pending: move on vs stay on GC2 pooling.

## EOY Position-Player report — hitting half done + Leg 2 started (feature/pd-goals)
- Finished HITTING half (P1-12, synthetic-layout). P10 POC×LA + EV×POC regression; P11 POC overhead bubble + POC density (hitter_analysis Ball-Flight contour, no dashed POC-0 line); P12 glossary; P7 REDESIGN — Season Rolling Values (Contact%/ZCon%/OSw%, whole season from first pitch) on top, swing-density below.
- **POC 0 = plate front edge** locked (`hit_initial_contact_point_y*12-17`, +=out front). OZCon% BACKLOGGED (CSC is a model, not a blanket zone) — OSw% substitutes for now.
- Started **Leg 2** (real data). Decision: reuse canonical where it lives (hitter_analysis.py + hitting tracker pins), thin layer over the engine like the fielding progression report. Wrote canonical-source map doc + eoy_data.py foundation (identity/Page1 + pure transforms for P4/9/10/11). DB port is WORK-LAPTOP only (no DB here; can't guess columns).
- **4 legs:** 1 foundation (done) · 2 hitting output (in progress) · 3 fielding half · 4 app + coach note/pin wiring (deferred).
- Zac says to remember: **BBC/college players** get EOY reports too (drafted, no affiliate yet). Both-source players = TWO separate same-looking reports (college + pro), never blended, key (gcid, data_source). BBC path via **/amateur** + amateur-data-guardrails. College pool decision OPEN.
- Next session: Zac on WORK laptop; I tell him what to run (port `_fetch_bip_df` first → lights up P4/9/10/11).
