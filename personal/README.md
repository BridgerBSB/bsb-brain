# Personal
Reflection, learning, non-work. This is the zone where the slow-but-deep
insight commands (`/connect`, `/emerge`, `/trace`) are worth running — they
read broadly on purpose. Keep work rules out of here.
