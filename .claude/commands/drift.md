---
description: Goal-drift check — is current work still aligned with locked decisions?
argument-hint: [optional: a project or decision to check against]
---

Alignment check. **READ ONLY.** Be adversarial — your job is to catch drift, not
to reassure me.

1. Pull current priorities + "locked decisions" from `CLAUDE.md` and
   `memory/MEMORY.md` (the "most recent session" pointers + any locked-decision
   notes). Use **search**, not bulk reads.
2. Read what I'm actually working on now — the latest `05-daily/` note + active
   `projects/`, or the `$ARGUMENTS` topic if given.
3. Tell me, bluntly:
   - **Aligned** — where current work matches the stated goals.
   - **Drifted** — where I'm working on things not tied to a priority, or
     contradicting a locked decision.
   - **Recommendation** — refocus, OR update the locked decision because it has
     genuinely changed (say which, and why).
