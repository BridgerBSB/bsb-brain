# Last session state - 2026-08-20 10:32 (Injury Tracker: level picker + 2 club analyses + PDF; PAUSED by Zac, not blocked)

- **Project / cwd:** `C:/Users/Owner/bsb-resources` - branch `feature/pd-goals`.
- **Recall checkpoint (SOURCE OF TRUTH):** session `411a` - domain `bsb-resources/feature/pd-goals` - id `79e61157f65fa22f`. Ring buffer 2/10.
- **What we were doing:** Finished the Injury Tracker app work (specific-MiLB-level picker), then built two club-level analyses Zac asked for - lower-body injury share, and 60-day IL reinjury - plus a 2-page landscape PDF with a column key. Zac closed the thread: *"we are good on the injury stuff at the moment and will come back later with more."*
- **Shipped this session:** `fb198582` (MiLB level picker - popover under the pills) - `3565d92b` (`src/injury_analysis.py`: `lower_body_share` + `sixty_day_returns`, ROK..AAA, rank 1 = best) - `95a69646` (`src/injury_pdf.py`: 2 landscape pages + key panel) - `ac2d4b05` (retitle p2 -> "60-Day IL: Reinjured Days"). Guards: `test_club_injury_queries.py` 38 checks + 6 injections, `test_club_injury_layout.py` 11 checks + 3 injections. All green, all DB-free.
- **EXACT next step:** Zac's own words - *"clean up any open or loose ends on documentation and move on to other projects."* The concrete loose end is the one carried over from the 2026-08-19 wrap and STILL not done: put the **rsconnect `--app-id`** lesson into `.claude/rules/new-posit-app.md` (and/or `pin-deploy-runbook.md`), then `bash C:/Users/Owner/bsb-resources/.claude/scripts/sync-rules.sh`. ~10 min.
- **Blockers / waiting on:** Nothing blocking. Injury thread paused by choice. The two new queries are UNRUN vs the live DB - Zac triggers that when he wants numbers (`--list-parts` FIRST, then `--season 2026`).
- **Uncommitted work:** 72 paths, all pre-existing untracked clutter from other threads. Everything this session is committed and pushed.

### Worth keeping
1. **There is no standalone `.sql` for either query, deliberately.** `days_missed` is the output of the `get_stints()` state machine over `TR_HISTORY`; `body_part` only exists in `SportsMed.DL_Stints`; the two are joined in Python by `(gc_id, closest placement date)`. Re-deriving either in T-SQL is a second definition of the dashboard's core metric, and the day they drift these numbers stop matching the dash while both still look right. Both analyses reuse the app's own functions, so Zac's **411** reproduces by construction.
2. **The render caught two bugs no test could, and one was analytical.** The key panel had a fixed height and page 2 carries one more note than page 1, so its last line drew below its own box. Worse: **zero-return clubs rendered `--` on GREEN** - best-in-league when it means no data. Two causes: a club with no 60-day returns has `post_days = 0`, which ranked it **first** on the raw total ("best at returning players" for never having returned one), and the PDF painted a rank colour behind a blank cell. Blocking rule #19 in both halves.
3. **Two of three layout injections went GREEN first, and each was a defect in the CHECK.** The panel check was catching the header's page label (so the fixed-height injection passed for the wrong reason); the guard `from`-imported `key_panel_extent` so patching the module never reached it; and the blank-cell injection patched the DATA when the guard reads the TEXT. A green injection is a finding about your check.
4. **`bbox_inches="tight"` grows the canvas around a runaway label**, so a saved PNG is structurally unable to catch the out-of-bounds case. The layout guard measures `Text` window extents instead - pairwise overlap scored against the SMALLER box, plus out-of-figure, plus containment in the key panel.
5. **Streamlit GCs the state of any widget it did not render.** The MiLB level picker is conditional AND inside a popover, so without a non-widget mirror key the pick silently resets to all-levels on every flip to MLB. Proven by a lifecycle sim + a no-mirror injection.

### Open, Zac's call (not blocking)
- **`Typed` column label.** He asked what it meant; I proposed `Dx` / `Diagnosed`; he said "everything else is chill" so it was left as-is. One-word swap across PDF + console + key.
- **Body-part labels unverified.** `LOWER_BODY_ALIASES` was configured from what Zac read off the app dropdown, not from eBIS. `--list-parts` prints any unresolved label by name; an unresolved one contributes ZERO and quietly shrinks the numerator.
- **App not redeployed** since `fb198582`: `$env:INJURY_TRACKER_APP_ID = "<GUID>"` then `.\pd-goals\injury_tracker\deploy.ps1`.

### Flagged, NOT yet durable (carried from 2026-08-19 - now the top of the queue)
The **rsconnect `--app-id`** lesson lives only in `pd-goals/injury_tracker/DEPLOY.md` + that app's `LINEAGE.md`. `rsconnect deploy` matched by `--title` does NOT identify existing content, so a deploy from a fresh clone silently creates a **second** app with an empty Vars tab - which presents as an SSPI/Kerberos DB error, not as a duplicate. Any sibling app deploying by title alone can still duplicate itself. Belongs in `.claude/rules/` + the 4-worktree sync.
