# Last session state — 2026-07-30 14:55 (PD Goals cockpit — LIVE + self-maintaining)

- **Project / cwd:** `C:/Users/Owner/bsb-resources` · branch `feature/pd-goals`
- **What we were doing:** Got the PD Goals org cockpit actually deployed and self-maintaining. Unblocked the work-laptop deploy chain (my deploy script failed 4x, 4 different bugs), built the missing Connect-scheduled bundle for the compliance-history pin, then fixed three UI reads Zac caught on the live page.
- **Shipped this session:** `4d53522b` + `68ae0861` + `ab0367b3` (deploy_pd_goals.ps1: manifest.json arg, --server/--api-key/--title, cwd survives the pin bundle) · `ce086675` + `3b5b112e` (NEW `connect_pins_history/` bundle → Connect content `pd-engine-pin-compliance-history-2026`, GUID `5925205b-777d-468e-9682-e15721f3e674`) · `e4befb85` (By Domain panel height = matrix height) · `542f0af0` (month trend reads April/May/June/July; open-player StreamlitAPIException fixed) · `42b2fce2` (LINEAGE). 45 tests pass. Renders committed at `docs/plans/mocks/pd-goals-cockpit/phase4-*.png`. Work laptop chain ran GREEN — history pin built, 968 rows.
- **EXACT next step:** Delete the Goal Compliance tab Zac asked for — but PORT FIRST: move the "↻ Recompute current phase (live)" button into the cockpit header and fix the caption at `src/cockpit.py:591` that tells users to click it, THEN remove the `if _view == _V_COMPLIANCE:` block in `pages/1_PD_Goals.py` (~line 1189) + its `_V_COMPLIANCE` entry in the view radio. Ask Zac whether the per-GOAL Individual Compliance table needs porting or can be dropped (cockpit's table is per-PLAYER).
- **Blockers / waiting on:** Connect UI clicks on the work laptop — (1) `DB_USER`/`DB_PASS` Vars on `5925205b-...` (CONNECT_API_KEY NOT needed, Connect injects it, proven); (2) Schedule tab on BOTH pin contents, every 12h, timezone **America/Chicago** (not literal "CST"), staggered 1-2h with compliance first; (3) confirm `pd-engine-pin-compliance-2026` (`c65342b2-828c-4f6a-a9dd-916fe9caa3be`) even HAS a schedule. Also never run: the acceptance check (cockpit matrix Total vs Goal Compliance Total, same Year+Phase) — do it BEFORE deleting that tab, it's the cross-check surface.
- **Uncommitted work:** 84 files, ALL pre-existing/unrelated (slide-deck scripts, eoy-catching png, untracked `.claude/` scaffolding). Nothing of mine.

## ALSO OPEN — BR 1→3 canonical rollout (concurrent thread, same branch)
Commits `06fcfa58` `5ee45396` `aa006003` `ff3e3bf5` `ac171cc6` landed on `feature/pd-goals` during this session but belong to a DIFFERENT thread (BR true 1→3 on EOY P21 + Org KPI, diff harness, scaffold-pin-deploy notebook-parser fix). Do not attribute them to the cockpit work.
